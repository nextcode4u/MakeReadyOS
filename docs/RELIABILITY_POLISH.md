# Reliability And Polish Queue

Updated: 2026-09-06. Prioritize existing daily workflows over new modules.
Checked items mean local implementation and verification, not deployment or field validation.

## P0: Trust And Safe Saves

- [x] Restrict `/planning/capacities` to explicit public staff fields rather than returning full Prisma user records (including credential hashes). Filter capacity visibility by staff scope and restrict global capacity edits to managers covering all of the staff member's properties, or admins. Isolated tests cover credential omission, cross-property rejection, and permitted edits. This fix is not deployed; review exposure history and credential-rotation needs during release planning without assuming misuse occurred.
- [x] Restrict map-pin creator/editor relations to ID, display name, and role. The shared include previously returned complete user records through pin responses. A regression verifies password and reset hashes are excluded. Include this endpoint in the exposure review above.
- [ ] Distinguish incomplete pool inspections from successful checks: omitted safety checks currently become PASS, and missing chemistry can produce an OK evaluation. Preserve partial logs but require explicit observations before presenting compliance/safety success. Validate product-specific dosing, units, concentration basis, and operator-selected chemical separately from generic correction labels.
- [ ] Prevent duplicate offline creates after partial success: project/lease/pest sync currently creates the record and then uploads all files without checkpointing the returned ID or each successful attachment. Persist a create-to-upload transition, preserve remaining files, and add server idempotency for ambiguous network responses. Test interruption after create, after each upload, and before local queue deletion.
- [ ] Replace automatic `db:deploy || db:push` startup fallback after rehearsing migration history on a restored production backup. Failed migrations must not silently switch to schema synchronization; retain disposable-development recovery as an explicit opt-in command only.
- [x] Migrate Docker/CI/local tooling and package engines from end-of-life Node 20 to Node 24 LTS; add `.nvmrc`. Node 24.20.0 builds, Prisma generation/migrations, Alpine Chromium PDF rendering, and full integration suite passed (`logs/test-20260906-164021.txt`). Production-build browser validation remains a release gate. See [Node.js release support](https://nodejs.org/en/about/previous-releases) and [EOL policy](https://nodejs.org/en/about/eol).
- [ ] Audit refrigerant calculations and every report against its advertised scope: usage, both recovery categories, transfers, archived tanks, filters, and missing measurements. Compare outputs with known fixtures, not just successful downloads.
- [ ] Audit availability/directory reconciliation across properties, NTV occupancy, stale reports, missing units, and occupied-board cleanup. Preview must describe exactly what will change.
- [ ] Trace each save through response, refresh, activity history, and export. Distinguish loading, empty, failed, stale, and confirmed states.
- [ ] Audit dashboard date boundaries and metric labels: vendor overdue currently compares due dates with week start rather than today; planned work uses local-midnight bounds against date-only records. Preserve physical occupancy counting of both NTV variants. Add known-date, timezone, and unknown-occupancy fixtures.
- [x] Separate overview totals from recent-row samples. Project counts/cost totals use all scoped lightweight records; priority/upcoming/photo queries are filtered before their ten-card limits. PM totals use all lightweight task summaries and load details for at most thirty visible cards. Isolated 301-project and 201-task fixtures verify totals and late priority/completion records; full integration validation remains pending for this change.
- [ ] Make unit-history limits explicit or add pagination: its timeline silently caps audit events at 200, automation runs at 20 per turn, and final combined events at 250. Preserve permission checks and distinguish recent activity from complete historical records.
- [x] Replace the destructive global runtime-error screen with a dismissible notice outside the React root. Render errors use React's uncaught-error callback; details are text, not HTML. The isolated browser test covers preserved drafts, repeated errors, markup escaping, mobile width, dismissal, and cancelled reload.
- [x] Scope refrigerant controlled-form resets to the submitted form using its reset event. The local browser fixture verifies an unsubmitted charge draft survives a separate successful type creation.
- [x] Complete the full local `test.sh` suite with the dependency repairs below. Root/API/web audits, builds, and authenticated API workflows passed on 2026-09-06; the latest repeat includes unit-scope and lease-default initialization fixes (`logs/test-20260906-161042.txt`). Pushed GitHub CI is not yet verified.

## P1: Predictable Field Work

- [ ] Verify exact-unit matching and independent resolve/persist actions for every compliance issue in Quick Capture and Active Issues.
- [x] Give lease Quick Capture its own paginated exact-unit lookup, independent of the filtered 200-row Active Issues response. Mobile regression verifies multiple pages with an empty main list and resolving one issue without hiding the other. Resolved/archived issues are excluded from repeat cards; failed lookup shows retry feedback.
- [x] Extend independent paginated lookup to building/area-only grounds capture. API tests verify property scope and unit exclusion; mobile-sized browser checks find later-page cards with an empty Active Issues list. Pagination and cancellation across other capture modules remain queued.
- [x] Replace truthiness-based boolean query coercion across operational modules: URL values `false` and `0` no longer enable archive/inactive/recurring filters. Shared parser tests and custom-field archive lifecycle integration checks pass, including malformed-value rejection. Full integration pass: `logs/test-20260906-162321.txt`.
- [x] Correct planning's UTC date-only comparisons: same-day blocks disappeared when the API ran in a timezone behind UTC. UTC/Chicago/Auckland and DST date tests pass; the real-browser create-and-display check passes.
- [x] Enforce assignee property access in planning even for unrestricted admins, and revalidate the existing assignee when moving a block. Isolated route tests verify rejection before writes and correct 403/404 distinctions. Non-manager patchable fields and canceled-block visibility remain queued.
- [x] Keep expected login failures local to the sign-in form and block duplicate pending submissions. Browser regression verifies preserved inputs, inline error without global notice, and successful retry.
- [ ] Audit bulk archive/delete selection scope, confirmation, double-submit prevention, and restore access on mobile.
- [ ] Clear hidden board selections after filtering/property changes; snapshot confirmation targets and prevent repeated submissions. Keep a failed archive confirmation open with retry feedback rather than dismissing before the response.
- [ ] Standardize form grids, checkbox rows, dropdown portals, dialogs, and action rows. Add viewport and keyboard regression coverage before replacing module-specific CSS.
- [ ] Test durable local saves and attachment queues under disconnection, reload, retries, authentication expiry, storage failure, and duplicate submission. Do not clear capture until persistence succeeds.
- [ ] Bind offline jobs to their originating user and verified session, including automatic sync, manual retry, queue visibility, and account switches. Existing jobs have no owner; quarantine ambiguous legacy jobs without deleting unsent work. Add cross-account and in-flight logout tests before migration.
- [x] Share a same-tab per-job lock between manual retries and automatic sync. Three failing concurrency fixtures now pass, along with retry-after-rejection and earlier offline/storage lock-release checks. Queue deletion/failure updates are inside the shared lock.
- [ ] Extend offline delivery coordination across browser tabs and require server idempotency for retries after ambiguous responses. Same-tab locking does not prevent duplicate records after a lost response, partial upload, reload, or concurrent tab.
- [x] Clear all React Query entries on successful sign-out/sign-in, including session-expiry reauthentication. Same-tab admin-to-tech browser regression verifies prior cached lease cards do not appear while the new account's lookup is pending. Offline job ownership remains separate unfinished work.
- [x] Release the offline sync lock after offline exits and initial storage failures. Isolated tests verify a subsequent online or storage-recovered attempt can run; this does not yet cover durable attachment delivery.
- [ ] Remove redundant entry where prior values are reliable, and label gross/net/initial/remaining weights explicitly. Unknown must not silently become zero.

## P2: Simplification

- [ ] Review role-specific landing pages and actions with techs, leasing, and managers; hide irrelevant controls without weakening API authorization.
- [ ] Collapse optional context and empty related-record sections; keep primary capture and work actions visible.
- [ ] Validate report terminology and disclose scope/limits where completeness is not guaranteed.

## First Local Pass

- [x] Move refrigerant type/tank/charge/recovery/final-recovery form resets after confirmed saves. A local 412px browser fixture verified the type form retains values during pending/rejected writes and clears after success. Remaining form-specific field validation stays in the broader audit.
- [x] Add history loading/error/retry feedback. The local browser fixture verified failed history does not appear empty and retry recovers. Cached rows remain visible with the error message when available.
- Targeted authenticated browser tests for refrigerant failed saves/history, toolbar account/dialog behavior, and runtime-error preservation all passed against the isolated local test database. A broader browser run exposed cross-origin edit failures and stale test selectors; reruns are in progress. Web production build and `git diff --check` passed.
- [x] Fix full-audit property-label initialization, remove the 1,000-transaction export ceiling, and include the union of all CSV section columns. Preserve property labels in the full-audit unit-history section. Use escaped/quoted spreadsheet output for formula prefixes, tabs, and line breaks.
- [x] Fix virgin-tank remaining-weight math and low alerts: remaining means net contents for virgin tanks, free capacity for recovery tanks. API tests cover full/low virgin tanks with tare and unchanged recovery headroom.
- [x] Capture Pest Quick Add/vendor form elements before awaiting saves, and reset lease issue-type setup only in its success callback. Lease issue-type failures now stay beside the form, and pending saves disable the submit button. Authenticated mobile Pest and Lease setup failure/success browser checks passed.
- [x] Allow PUT/PATCH/DELETE CORS preflights for trusted origins. Browser column-label edits, photo markup, and operating-calendar saves now pass across separate web/API addresses. Untrusted origins remain denied; explicit preflight checks are included in `test.sh`.
- [x] Fill six missing English/Spanish translation labels and add a literal translation-key regression check.
- [x] Preserve typed search text when clearing a selected option or refreshing option objects. Stop map placement from immediately auto-selecting another unit during a search; retain automatic advancement after saved placements. Map upload/placement/reposition/drawer and unit-creation browser workflows passed.
- [x] Capture the map file input before awaiting upload, reset only on success, and show upload errors inline rather than creating an unhandled event error.
- [x] Block map upload during pending creation and unit search during pending placement, preventing delayed success callbacks from selecting an old map or overwriting the next search. Updated map browser tests pass with existing maps present.
- [x] Guard confirmation dialogs against repeated submission and show rejected actions inline. Keep failed board archive confirmations open; prune selections when rows leave the filtered board. Desktop failure/duplicate-confirm/filter tests pass; broader cross-property/mobile target-list review remains queued.
- [x] Move analytics comparison hooks before loading/empty returns. A delayed-response dashboard test reproduces and now prevents the hook-order crash. Full display-mode workspace checks and the mobile pool/pest/lease/project/PM/wiki/map workflow pass locally.
- [x] Ignore the API-local upload directory so runtime photos/documents are not accidentally committed.
- The isolated API report test uses 1,005 transactions across two properties and archived dirty inventory. It verifies CSV, spreadsheet, and HTML contents, late-record compliance errors, and property filtering. PDF rendering and real-database query behavior remain separate validation items.
- [x] Include legacy `RefrigerantLog` records separately, dismissed leak flags with resolution notes, and archived/disposition information in full audit. Disclose shared cylinder inventory versus property-filtered history. Isolated export tests cover these sections without double-counting legacy logs as modern transactions.
- [x] Correct stale refrigerant documentation (`TW` terminology, charge-source scope, calculation descriptions) and explicitly document the current recovery-ceiling estimate's limitations.
- [ ] Validate and replace the recovery-ceiling estimate using cylinder/refrigerant manufacturer requirements. Current WC/nominal-size math does not account for refrigerant density, temperature, or cylinder compatibility; do not treat it as a universal verified safe fill limit.
- [ ] Audit Pest capture interruption/partial-upload failure and repeated-submit behavior. Preserve the confirmed issue ID so recovery never requires recreating an already saved issue.
- [x] Apply compatible Fastify/fast-uri patches and web nanoid update. Replace the isolated Puppeteer PDF adapter with Playwright Core (existing Chromium executable, same paper/margin/header/footer options). Remove the vulnerable extract-zip chain. Scope a pinned deepmerge-ts 8.0.2 override to Prisma config; schema generation, all 45 migrations on a fresh disposable PostgreSQL database, and password lifecycle tests passed.
- [x] Verify multipage PDF text, headers, and page-number footers locally; verify PDF creation using Alpine's system Chromium in the API image. Root/API/web npm audits are now clean. Full CI and release verification remain separate gates.
- [x] Isolate `test.sh` under a unique Compose project before its volume cleanup and validate Compose configuration without writing secrets to logs.
- [x] Isolate both test scripts from deployment `.env`, inherited database/upload paths, and Compose project/file settings. Browser script now cleans up only its unique disposable project and runs shared-fixture tests serially; environment-isolation shell regression passes.
- [x] Complete the full desktop/mobile browser run against a fresh demo database: 63 tests passed on 2026-09-06. API integration suite also passed with planning, boolean filters, and unit lookup fixes (`logs/test-20260906-163104.txt`). Node 24 release validation remains in progress.
- [x] Reject blank/null/boolean/nonfinite required refrigerant weights rather than silently coercing them to zero. Numeric zero and optional null tare values remain supported.
- [x] Reject virgin/archived recovery targets, clean/dirty category mismatches, clean-refrigerant type mismatches, unrelated extra tank IDs, same-tank transfers, and repeated final recovery. Isolated API validation tests pass.
- [ ] Move refrigerant tank validation and inventory writes into a locked transaction; test concurrent charges/recoveries and stale scale baselines. Also verify unit/property consistency and permissions for administrative tank corrections.
- [ ] Surface gross readings below stored tare as discrepancies instead of silently clamping negative contents to zero. Distinguish measured from inferred tare and provide an audited correction path before deciding which legacy readings should block new entries.
- [ ] Model clean-recovery provenance and ownership eligibility explicitly. Current charge validation checks category, active status, and refrigerant type, but shared cylinder records do not establish originating equipment/ownership or validate reuse eligibility. Review the applicable requirements before claiming the workflow enforces them.
- [x] Validate refrigerant unit IDs against their directory property and canonical number. Isolated API tests cover missing records, mismatched properties, denied unit scope without a supplied property, and canonical values on successful recovery.
- [x] Make default lease issue-type creation duplicate-safe under simultaneous initial requests. Ten concurrent requests against PostgreSQL all returned the same unique default types.
- [ ] Add immutable tank-edit history with before/after weight, tare, capacity, category, and refrigerant values plus correction reasons. Include lifecycle events in audit exports; existing generic update messages cannot reconstruct historical tank configuration and must not be presented as doing so.
- [x] Add React hook-order linting to the CI test path and verify the rule rejects an early-return fixture. It found and fixed a second conditional-hook bug in the dashboard throughput section. Broader dependency/React Compiler lint adoption remains a separate cleanup.
- [x] Clear cached API responses on logout/account changes/authorization failures, exclude admin/export responses, ignore late prior-session responses, and preserve fresh responses on quota failures. Isolated worker tests and a real service-worker-enabled mobile sign-out/offline check pass.
- [x] Complete the Node 24 production-image browser run: 66 desktop, mobile, visual-capture, and PWA checks passed (`logs/e2e-20260906-165313.txt`). Mock-driven tests block service workers; the dedicated PWA test explicitly enables them.
- [x] Repeat the production-image browser run with common-area pagination, same-tab account switching, sync-lock repair, report cutoff fixes, and health-gated startup: 67 checks passed (`logs/e2e-20260906-171735.txt`). Planning/map privacy fixes were made after that image snapshot and have separate regression coverage.
- [x] Repeat production-image desktop/mobile/PWA validation with planning/map privacy, pool chemical validation, error/retry states, and spreadsheet fixes: all 69 checks passed (`logs/e2e-20260906-174428.txt`). Project/PM complete-total queries were added after that image snapshot and have focused coverage pending broader validation.
- [x] Keep building/area-only repeat matching separate from unit-specific issues. Pure matcher tests cover wrong units in the same building, cleared selection, common areas, and resolved/archived exclusion.
- [x] Reject delayed identity responses after sign-out and stop offline cache reads when cache deletion fails. Six isolated service-worker regressions pass.
- [x] Render and inspect the complete refrigerant audit PDF with 1,005 transactions, archived dirty recovery, legacy logs, and dismissed flags. Landscape page dimensions and extracted contents pass; raw text extraction preserves wrapped cell identifiers.
- [x] Remove silent issue/log cutoffs from lease, pest, and pool HTML/PDF reports. A 251-record fixture reproduces the prior failure and now verifies the final matching record in all nine CSV/HTML/PDF outputs while preserving property filters. Reports remain operational summaries, not complete attachment or lifecycle histories.
- [x] Validate pool chemical references against the active property catalog, reject incompatible measurement units, and store canonical chemical names. Pure and route tests cover cross-property/unknown IDs, invalid liquid/solid units, gallons/quarts, and equivalent pounds/ounces saves.
- [x] Repeat the full API integration suite with report completeness, planning privacy, map privacy, and custom-port configuration checks: `logs/test-20260906-172307.txt`. Pool chemical validation was added afterward and has separate focused coverage pending the next full run.
- [x] Wait for API HTTP health before starting the web proxy and keep container port 4000 independent of the published host port. Fresh default-port integration (`logs/test-20260906-171145.txt`) and an isolated 14999/18080 startup/proxy check pass. Host boot validation remains separate.
- [x] Remove the daily manager report's silent 500-event cutoff. A 501-event regression verifies JSON/CSV completeness, date boundaries, and property authorization.
- [x] Escape spreadsheet formula prefixes in daily, lease, pest, pool, project, PM, board, map, and charge CSV exports. Project/PM/map tab-delimited spreadsheet exports now quote embedded tabs, newlines, and quotes; route fixtures verify hostile and multiline text, including grouped charge reports.
- [ ] Prevent custom-field labels from overwriting built-in board CSV keys or other same-label custom fields. Define stable, unambiguous export headers with compatibility tests before changing consumer-visible names.
- [x] Full local API integration passed again (`logs/test-20260906-174112.txt`), covering pool validation, daily completeness, and project/PM export updates. The subsequent board/map/charge escaping changes have focused route coverage and await the next full integration pass.
- [x] Full API integration passed with board/map/charge spreadsheet escaping and complete project/PM overview totals (`logs/test-20260906-175014.txt`). Same-tab retry locking was added afterward and has six focused orchestration regressions.
- [x] Distinguish failed pool-history and project-list requests from empty results, preserving cached rows and offering retry. Both 412px browser failure/retry regressions pass.
- [ ] Review bounded risk evaluation (500 items) for explicit scope/truncation disclosure or complete batched processing. Keep intentional overview limits distinct from full reports.
- [x] Reject risk-evaluation item IDs that do not belong to an explicitly selected property, even when the caller can access both properties. Manager/admin mismatch tests reject before evaluation; a matching request still reaches the evaluator.
- [x] Make audit IP attribution honor `TRUST_PROXY`, using the same helper as authentication. Untrusted forwarding headers no longer override the request IP in audit records; tests cover trusted/untrusted modes and background events.
- [ ] Restrict proxy trust to the deployed proxy topology rather than trusting arbitrary forwarding headers whenever a boolean is enabled. Review exposed API ports and Cloudflare-to-origin forwarding before rollout; generic proxy-enabled behavior remains unchanged by the audit fix.
- [ ] Add report-size/load testing and an asynchronous export path for exceptionally large histories; never silently truncate matching records as a memory workaround.

Dependency patch references: [Fastify advisory](https://github.com/advisories/GHSA-w2qp-rph6-63g4), [fast-uri advisory](https://github.com/advisories/GHSA-5jgf-p345-68v8), [nanoid advisory](https://github.com/advisories/GHSA-2v37-7h3g-55p8).

Compatibility references: [deepmerge-ts 8 changes](https://github.com/RebeccaStevens/deepmerge-ts/releases/tag/v8.0.0), [Playwright PDF API](https://playwright.dev/docs/api/class-page#page-pdf). The Prisma override is deliberately limited to its config loader; reassess it when upgrading Prisma.

## Release Gate

- Release preparation on 2026-09-06: final API integration, builds, audits, and actual Chromium PDF regressions passed (`logs/test-20260906-180406.txt`). A test-only readonly-config assignment was corrected before this pass. Production database/uploads backups were verified before deployment; all 45 migration checksums match, with pre-existing live-schema differences queued for separate reconciliation. This release changes no Prisma schema or migration files. Final 73-test production-image browser verification is running before server cutover.
- Paused at user request on 2026-09-06. All changes remain local and uncommitted; nothing pushed or deployed during this audit. Latest full API integration: `logs/test-20260906-175014.txt`. Latest full production browser run: 69 passed (`logs/e2e-20260906-175233.txt`), including complete project/PM totals and same-tab retry locking. Subsequent risk-scope and audit-IP fixes pass focused tests. PM calendar/tasks/templates/history now distinguish loading/failure from empty results, preserve cached rows, and offer retry; all four 412px browser regressions passed. Lint and diff checks pass. Run the full integration/build/browser suite again for these final changes before release.
- [ ] Build and run targeted regression tests for each change; record untested scenarios explicitly.
- [ ] After server hardware work is complete, verify boot, installed RAM, app health, and field workflows before deployment.
- [ ] Preserve database/uploads backups before any data or schema changes. No production mutations during local auditing.
- [ ] Audit host boot management against Compose restart policies before changing restart behavior; avoid competing host-level supervisors. API health checks verify HTTP startup, not continuous database connectivity. See [Docker restart policy guidance](https://docs.docker.com/engine/containers/start-containers-automatically/).
