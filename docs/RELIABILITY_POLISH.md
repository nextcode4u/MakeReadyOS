# Reliability And Polish Queue

Updated: 2026-09-14. Prioritize existing daily workflows over new modules.
Checked items mean implementation and verification; see the release checkpoint below for deployed coverage. Field validation remains separate.

## Approved Module Roadmap

On-call module: see [Shared On-Call Workspace](ON_CALL.md) for the user-approved public-schedule/protected-guide access model, implementation scope and follow-up queue. This is a local implementation, not a deployed feature yet.

Approved by the user on 2026-09-14. Recommendations are pending work, not a new verification of deployed behavior. Reuse the existing tasks listed below rather than creating parallel implementations. Order: shared status consistency, issued inspection reports, reliable photos/offline recovery, trade handoffs, then Projects comparisons/reminders. Off-server recovery remains a parallel infrastructure priority.

Existing task coverage:

- **Board / Table:** One shared unit-status rule, cross-screen regressions and readiness provenance in Next Priorities.
- **My Work / Assigned Work / Unit Details:** Prominent next action, explicit painter/cleaner handoffs and the Unit Work Plan checkpoint. Keep confirmed work ahead of projections; lead with the current step in initial walk -> repairs/parts/keys -> painting -> cleaning -> final walk, without hiding relevant details.
- **Initial Walk / Photos:** Per-unit upload confirmation and photo reliability prerequisites. Only server-confirmed evidence can be described as backed up; resume safely without duplicate photos/issues.
- **Final Walk:** Verified report issuance, immutable revisions, durable reviewer/technician identities, packet preflight and failed-inspection corrections. Keep drafts distinct from issued evidence.
- **Calendar / Planning:** Schedule capacity warnings, explain scheduled dates and rescheduling consistency. Preview downstream changes and preserve confirmed/manual dates unless explicitly changed.
- **Projects:** Use the [Projects follow-up queue](PROJECTS_WORKFLOW_AUDIT.md#follow-up-queue) for scope-based quote comparisons, expiry/deadline reminders, associating existing PDFs, durable uploads and concurrency.
- **Setup / Imports:** Property-specific Added / Updated / Skipped / Archived review and guarded undo. Keep availability, unit directory and mailbox setup together; verify existing navigation before adding controls.
- **Accounts / Notifications:** Existing read-only View as user and notification noise-reduction tasks, including quiet hours and urgent handoffs.
- **Backups:** Off-server recovery protection and Backup Reliability Follow-Up, including encrypted paired database/uploads backups, coordinated restore and tested recovery.

Additional scope to implement or incorporate into the related task:

- [ ] **P2: Parts partial receipts and order queue.** Extend the per-unit shop list with quantities requested, ordered and received. Make Need to order visible in a manager/admin action queue without duplicate notifications. Ordinary pickup reminders remain non-blocking; do not introduce the previously declined combined shop pickup list. Extend the parts task in [Turn workflow audit](TURN_WORKFLOW_AUDIT.md#blocking-follow-up).
- [ ] **P2: Explain automation outcomes.** Show the relevant unit/property rule, assignment split, triggering data and reason for applied or skipped actions in plain language. Distinguish historical execution evidence from a current-rule explanation; never invent an explanation for an unrecorded historical decision.
- [ ] **P2: Vendor context and duplicate-safe capture.** Extend the Projects vendor procurement task with optional inline contact details, duplicate detection and links to associated quotes, projects and scheduled work. Respect property access and preserve existing vendor associations.
- [ ] **P2: Refrigerant reconciliation view.** Extend the existing calculation/report audit with starting contents + additions - removals = expected remaining, modeling each usage, recovery and transfer direction explicitly. Explain discrepancies and distinguish measured from inferred weights; verify against known transaction fixtures without double-counting transfers.
- [ ] **P2: Pool trends and follow-up.** Extend product-specific dosing/unit verification with unmistakable selected product, concentration and units, reading trends, and assigned follow-up for exceptions. Preserve unknown readings and avoid presenting generic correction suggestions as product-specific guidance.
- [ ] **P2: Baseline pool correction and sequencing guide.** User clarification: keep useful generic correction suggestions, not just product-specific dosing. Explain the general next action for a single out-of-range reading and evaluate combinations, especially low free chlorine with high pH, to recommend a reviewed order of operations rather than independent chemical-addition prompts. Spell out free chlorine (FC), combined chlorine (CC), and calcium hardness (CH); low CH must not be interpreted as low chlorine. Separate general guidance from calculated product-specific doses. Before implementation, validate decision rules against authoritative pool-operation guidance and product labels, accounting for relevant readings, sanitizer type and missing inputs. Explicitly warn against mixing acid and chlorine or adding them simultaneously; show separate steps with label-specific circulation/retesting requirements, not a universal wait time. Include applicable pool-closure/reopening guidance without treating chemical addition alone as evidence that swimming is safe. Test combined exceptions, contradictory/missing readings, different products and units, and recommendations after retesting; this TODO does not establish an unverified acid-first or chlorine-first rule.
- [ ] **P2: Pest / Lease next action.** Make responsible person, next action and follow-up date clear for every open issue. Verify existing reminders before extending them; resolution must retain evidence, history and property scope.
- [ ] **P2: Preventive-maintenance recurrence clarity.** Distinguish missed work from future occurrences and test repeated/concurrent scheduling for duplicate tasks. Define reschedule, completion and cancellation behavior without erasing historical occurrences.
- [ ] **P2: Wiki / Maps field access.** Make equipment documents and service history reachable from the relevant unit or map pin with minimal navigation. Reuse existing relationships, respect access controls and collapse empty context rather than adding more panels.
- [ ] **P2: Dashboard / report drill-through.** Link metrics to the underlying records using the same property/date/status scope and disclose partial coverage. Extend completion-date auditing and report-scope work; distinguish verified completion events from inferred dates and preserve filters in downloads.
- [ ] **P3: Pond discovery continuity.** Extend the existing cross-device discovery task with explanations of earned milestones. Keep cosmetics optional and noncompetitive, avoid incentives to mark incomplete units ready, and distinguish server-verified progress from local discoveries.

## Projects Usability: 2026-09-14

Deployed in `32129c6`: structured multi-vendor/multi-scope quotes, multiple PDFs per quote, in-house labor/material cost lines, inline vendor creation, deadline editing, previews/ZIP downloads, report/export totals and native backup coverage. Existing manual bids and amounts remain intact and separate. See [Projects workflow audit and follow-up queue](PROJECTS_WORKFLOW_AUDIT.md) for intended behavior, limits, tests and remaining recommendations.

Verification: `logs/test-20260914-075557.txt` passed the full isolated/API/database/build/lint/dependency gate. Subsequent Projects browser regressions passed 11/11 (`/tmp/mros-project-existing-browser.log`). The final commercial workflow and desktop/mobile layout check passed 1/1 (`/tmp/mros-project-browser-final.log`), including multiple quote PDFs, included-only amounts, internal actuals, optimistic deadline saves, ZIP/PDF/CSV downloads, native backup restoration/replay, rejected foreign quote links, and missing-file ZIP rejection. Final production-image builds and lint pass; screenshots were inspected.

Deployment checkpoint: application `32129c6` is live with matching build metadata; GitHub CI run `34847734877` passed. The additive migration applied successfully in production. API/database containers are healthy and web is running; public page and entry JavaScript, authenticated identity, Projects overview/list and logout returned 200. Production has no project records, so commercial mutations and exports were exercised only in isolated tests. Readable predeployment backups are `backups/makereadyos-db-20260914-081206.dump` and `backups/makereadyos-uploads-20260914-081217.tgz` on the server; the upload volume is currently empty. Rollback images retain `93373c7`. The migration hygiene audit confirmed applied checksums and identified pre-existing unrelated schema drift, which was left untouched.

## Release Wrap-Up: 2026-09-13

Feature work is paused at the user's request. This batch improves shared readiness/risk labels, transactional availability receipts and freshness, mobile parts/report/code draft recovery, per-file upload outcomes, unit work-plan/next-step visibility, filtered unit opening, responsive filters, analytics completion eligibility, and backup failure handling. Larger status, signed-report, turnover-history, upload-idempotency and off-server recovery projects remain open below; partial work is not marked fully complete.

Verification: the broad browser checkpoint passed 197 cases before the later follow-ups; the final drawer/parts/report/code production-image checks passed 4/4 in `/tmp/mros-item-filter-after.log`. Earlier mobile filter/code checks passed 2/2 separately. Final full local API/database/build/lint/dependency gate passed in `logs/test-20260913-214323.txt`, including the direct-item loader. No schema/migration files change in this release. Server predeployment database/upload backups were created at 21:43 CDT; dump contents are readable, and the upload archive matches the currently empty mounted upload directory. Rollback API/web images retain `6f3be84`.

Application commit `93373c7` is pushed and deployed to `mr-os.com`. API/web containers passed Compose readiness; the database remains healthy, with 57 migrations and none pending. Public page and entry JavaScript return 200; anonymous auth correctly returns 401. Authenticated public read-only checks passed for auth/me, availability freshness, Assigned Work, unit details and the new unit work-plan endpoint. Existing administrator credentials were used without changing passwords or work records; the old bootstrap password did not authenticate. Docker, `makereadyos@mros`, and Cloudflare Tunnel remain boot-enabled. GitHub code CI is still completing its test step at this checkpoint. Earlier local-only notes below are historical verification checkpoints, not the current deployment state.

## Next Priorities: Status Consistency And Usability

Added at the user's request on 2026-09-13. Start with the first two items together; these are pending work, not completed features. Older release notes below remain historical.

- [ ] **P1: One shared unit-status rule.** Define repair completion, whole-unit readiness, occupied/archive, and down/model eligibility consistently across the API, board, calendar, My Work, reports, risk, and pond. Explicitly cover availability-import readiness, independent final-walk approval, managerial overrides, and reopened turns without inventing completed trade or inspection records.
- [ ] **P1: Cross-screen workflow regression tests.** Verify the user-visible technician repairs -> painting -> cleaning -> final walk -> ready -> occupied/archive lifecycle on desktop and mobile, with technician, leasing/inspector, and manager/admin roles. Include imported-ready units with blank historical trade fields, overrides, full versus partial availability imports, stale previews, down-unit exclusions, and agreement across screens. Test real persisted outcomes as well as labels and colors.
- [ ] **P1: Finish verified resident report issuance before replacing signed paper records.** Carry forward the existing blockers in [Turn workflow audit](TURN_WORKFLOW_AUDIT.md): durable technician/reviewer identities, independent sign-offs, immutable approved report revisions, and explicit correction/reopening rules. Snapshot issued branding, mailbox and resident-specific handoff details. Current previews/PDFs remain drafts; imported or overridden readiness must never fabricate inspection evidence.
- [ ] **P2: Show readiness provenance.** Display why a unit is ready: availability report and report date, final-walk approver and timestamp, or manager/admin override and reason. Preserve the distinction between imported or overridden readiness and an inspection actually performed.
- [ ] **P2: Import review and guarded undo.** Replace confirmation popups with a property-specific Added / Updated / Archived / Skipped review. Add auditable import recovery that restores only unchanged imported values, preserves newer work, and presents conflicts instead of silently overwriting them. Build on the existing full-report archive preview and transactional safeguards.
- [ ] **P2: Prominent next action.** Put the next required step, responsible person or vendor, and expected date at the top of each unit's working view, such as "Repairs finished; waiting for painter." Keep full details available below and handle unassigned, unscheduled, blocked, ready, and down states explicitly.

## Additional Approved Recommendations

Added on 2026-09-13. Keep status consistency and its regression tests first, then prioritize availability freshness. The combined shop pickup list was declined: technicians normally work one unit at a time, so parts lists should remain per unit.

- [x] **P2: Availability freshness indicator.** Show each property's last successful availability import and report date, distinguish full from partial reports, and clearly flag stale or unrecorded imports before users rely on the board or schedule. Implemented, verified and deployed in `93373c7`. Legacy receipts explicitly retain unknown coverage/dates.
- [ ] **P2: Schedule capacity warnings.** Detect overlapping or over-capacity technician assignments using expected work durations and property working days. Suggest alternatives without silently moving confirmed dates or assignments.
- [ ] **P2: Notification noise reduction.** Bundle routine updates into an optional daily summary while preserving immediate notifications for assignments, inspection handoffs, missing orders, and urgent move-in conflicts. Respect account preferences and quiet hours, and avoid duplicate delivery.
- [ ] **P2: Resident packet preflight.** Before printing/exporting, flag missing mailbox details, expected property branding, applicable resident-specific codes, and unanswered inspection checks. Distinguish not-applicable fields from missing information and actual inspections from imports or administrative overrides; never expose staff/master codes.
- [ ] **P2: Per-unit photo upload confirmation.** Display uploaded, pending, and failed photo counts within the unit, clearly distinguishing server-confirmed evidence from files stored only on the phone. Keep retries, offline recovery, and ZIP-export inclusion understandable without duplicate uploads or lost photos.
- [ ] **P2: Off-server recovery protection.** Verify existing backup coverage before adding anything; ensure encrypted off-server copies include database, uploads, and required recovery configuration. Add backup-failure alerts and periodic isolated restore checks with recorded results. Keep recovery keys separate and do not count backups stored only on the app server as protection against losing that server.

## Further Approved Workflow Improvements

Added on 2026-09-13. Implement after the shared-status foundation and regression coverage; verify existing behavior before adding duplicate features.

- [ ] **P2: Separate turnover history.** Keep photos, resident-specific codes, materials, assignments, and inspections attached to their original turnover. Audit new-turn creation and reopening so prior occupancy evidence and deadlines do not silently carry forward.
- [ ] **P2: Failed-inspection correction loop.** Let inspectors flag specific findings with photos, return corrections to the technician's My Work, and recheck those findings with an auditable history rather than repeat or overwrite the original inspection.
- [ ] **P2: Explicit painter and cleaner handoffs.** Distinguish ready for trade, scheduled/accepted, and finished; show responsibility and the next handoff without marking the entire unit ready or creating excessive notifications.
- [ ] **P2: Safe practice property.** Provide sample units for staff to rehearse initial photos, repairs, parts, and final inspection. Clearly identify practice mode and isolate real records, notifications, exports, and reporting totals.
- [ ] **P2: Explain scheduled dates.** Show whether a date is projected from expected vacancy, calculated from actual vacancy, or manually scheduled, along with applicable working-day rules and reasons for shifts. Do not claim a historical source when no provenance was recorded.
- [ ] **P2: Preserve unfinished mobile work.** Audit notes and line-by-line material drafts across navigation, reload, failed saves, and account/unit changes. Distinguish saved, unsaved, and upload-pending states and prevent drafts from leaking into another user's account or unit.
- Draft audit finding: parts drafts already persist per account/unit, but a stale batch currently offers only another rejected save or discarding its lines. Add an explicit review/rebase path that preserves new lines and newer server edits, detects already-saved IDs, and never silently overwrites a teammate's list. Existing conflict tests preserve input but do not exercise a successful recovery.
- Draft audit follow-up: `FinalWalkReportEditor` replaces its form whenever the outer query is fetching, and its unsaved answers exist only in component state. Reproduce an offline/reconnect or background invalidation while editing; preserve the mounted draft through transient refreshes without retaining access after an authorization failure. This is a code-identified risk, not yet a browser-reproduced fix.
- Photo audit follow-up: the direct multi-file upload loop aborts on its first non-network rejection, leaving later selections unattempted after the input is cleared. Add explicit per-file outcomes and retained retry candidates, distinguish server-confirmed uploads from device-only queue entries, and explain ZIP inclusion. Preserve account/unit scope and do not automatically retry permission or validation failures.
- Photo reliability prerequisite: attachment POSTs have no client upload identifier; a response lost after a committed upload can be queued and uploaded again. Add account/turn-scoped idempotent upload receipts before claiming duplicate-free retry behavior. Verify ambiguous responses against actual persisted attachments and ZIP contents; do not deduplicate merely by filename.
- Turnover-history audit finding (P1 follow-up): availability updates reuse the latest non-archived turn by unit ID/number. If a prior occupancy was never archived, a later notice/vacancy can reuse its turn-bound report draft, resident codes, photos and materials. Existing conflict review is not an explicit new-cycle decision. Add a guarded "new turnover vs correction of current turnover" review that preserves historical evidence and creates a fresh turn when confirmed; do not guess a new cycle or erase old records from dates alone. This is code-identified and needs a persisted regression before implementation.

## Active Status Audit Checkpoint

### Unit Work Plan And Next Action (In Progress)

- Fixed the drawer's work source: it previously used the default seven-day planning window and a global vendor list that was not enabled from Assigned Work. A property-scoped, staff-session-only unit endpoint now returns past and future non-canceled assignments, with explicit counts and truncation warnings. API-token access is denied because the response combines separately scoped planning and vendor data.
- Loading and failed refreshes no longer claim there is no assigned work. Transient errors retain labeled stale entries; access denial hides cached assignments. The focused working view distinguishes the repair technician from current-stage staff/vendor assignments and links to the detailed work plan.
- Next-step guidance separates repairs, painting, cleaning and independent inspection, without reopening operationally ready, down/model, occupied or archived units. It uses explicit known category matches, does not interpret generic QC as an inspector assignment, and labels scheduled work as plans rather than proof of completion. Custom categories and truncated history require review. Complete blocker-aware guidance, target dates without explicit work blocks, and future-notice preparation remain open under the broader next-action TODO.
- Isolated scope/date/truncation/token checks and two next-step regressions pass. API/web builds and lint passed before the next-step UI addition. The first mobile run failed in the test's navigation before reaching the drawer; it was corrected to use the mobile View menu. Updated focused browser and full local gates are running; no push or deployment.
- Follow-up browser testing found that Assigned Work's Open handler switched to Table, disabling the intended focused work drawer. Removed that forced view change so closing the unit returns to the work queue. Browser verification is in progress. The preceding full local gate passed in `logs/test-20260913-213145.txt`, including the unit endpoint and next-step logic/UI; it predates this navigation correction. A build-only failure from unsupported `replaceAll` in the web target was corrected before that passing gate.
- [x] Reproduce and repair direct unit opening when a board filter/window excludes the selected item. A scoped direct-item load now has explicit loading/error/retry and access-denial handling, retaining the originating queue and board filters. Final mobile regression verifies zero-result board search, successful unit opening, draft preservation on 503 and hiding unit details on 403; parts/report/code recovery regressions also pass (4/4).
- Unit work-plan/next-step verification passed: `/tmp/mros-item-plan-browser-v4.log` (1/1 mobile production-image test) confirms overdue and far-future blocks, unit-specific vendor work, stage-owner separation, transient-error labels, hiding cached assignments after 403, and returning to Assigned Work after closing. Web/API images and lint pass after the navigation correction. This is additional focused coverage after the full `213145` gate, not a new broad browser pass.
- The board-filter issue is now reproduced in `/tmp/mros-item-filter-before-v2.log`: a zero-result search leaves the Assigned Work card available but its Open action renders no drawer. Implemented a scoped direct-item query using the existing item-cache page shape so mutation invalidation and offline optimistic updates still cover it. Initial load/failure has an explicit dialog and retry; transient refresh failure retains the mounted drawer/drafts with a warning; 401/403/404 hides cached unit details. Updated mobile work/parts/report/code checks are running; keep the item unchecked until verification completes.

2026-09-13, local changes only. The broad P1 items above remain open; this is partial progress, not an end-to-end completion claim.

- [x] Reproduced and fixed trade status normalization: `NOT_NEEDED`, `not-needed`, and spaced variants now match the board's finished-trade display when deciding inspection eligibility. Unknown or unfinished values still block the handoff.
- [x] Reproduced and fixed ready-import risk disagreement: risk evaluation now uses the board's readiness predicate even outside a Ready section, preserving the explicit pending-final-walk exception. Completed/unnecessary trade variants no longer cause incomplete-cleaning or paint warnings.
- [x] Aligned pond readiness with board rules for whitespace, supported ready statuses, and pending final walks; repair completion alone is still not whole-unit readiness.
- [x] Reproduced and fixed stale work-view warnings: My Work and Assigned Work now compute current turn warnings on read using the board helper. Returned counters use those values; this does not change stored dates, assignments, or edit/automation timestamps.
- [x] Reproduced and fixed inspection scheduling for imported-ready turns. Scheduling now shares the API's operational readiness predicate while retaining a separate completion/approval predicate. A disposable PostgreSQL test verifies no new assignment for imported readiness, one assignment for finished trades, idempotent rescheduling, and cancellation without a fabricated report or approval after a subsequent ready import.
- [x] Consolidated frontend board repair labels, pond readiness, and move-in-risk filtering around one helper. A matrix checks 462 status combinations against the API contract, plus trade-completion variants. This is not yet a single implementation shared across both build targets.
- Verification: 31 targeted status/inspection/import/override tests passed for the first batch, with API/web builds and lint passing. An additional stubbed Fastify route regression passes for both work endpoints. Cross-module tests and the route regression are wired into `test.sh`. Focused real-database/browser handoff and upcoming-work checks are running; persisted workflow and broad shared-state migration remain unfinished.
- Browser/database checkpoints: `logs/e2e-20260913-185745.txt` passed 3/3; `logs/e2e-20260913-190043.txt` passed 4/4 including the persisted inspection scheduling regression. These precede the equivalent frontend-helper extraction; the full API/build/lint gate is running afterward.
- Follow-up found during this audit: My Work and Assigned Work still include completed assigned turns; review a clear completed-history treatment separately from open trade/inspection assignments.
- Availability freshness prerequisite found: the overall `AVAILABILITY_IMPORTED` audit is currently written after commit, risk refresh, and per-turn activity writes, and it does not record full/partial coverage. A post-commit failure can leave committed data without a reliable import receipt. Make the receipt transactional, record honest report-date/coverage metadata, and distinguish committed imports from refresh/notification failures before using receipts to promise freshness.

Status batch verification: `logs/test-20260913-190311.txt` passed the full isolated/API integration/build/lint/dependency gate. This checkpoint includes frontend readiness consolidation and precedes the import-receipt changes below. Nothing in this work session has been pushed or deployed.

### Availability Receipt Follow-Up

- [x] Reproduced failed receipt writes leaving listed-unit changes committed, then moved per-turn import activity and the overall receipt into the import transaction. Audit writes now accept an explicit transaction client without changing other callers' behavior. The stubbed transaction regression verifies rollback for both partial updates and full-report archiving.
- [x] Record versioned full/partial coverage, validated full-report date, and separately labeled source-row report dates in new receipts. Older receipts remain unchanged; do not infer missing historical metadata.
- [x] Return `applied: true` and visible warnings when post-commit risk/notification refresh fails, rather than returning a false import failure. Screen-refresh failures no longer reject a committed import mutation; the UI preserves its summary and instructs users to reload rather than repeat the import.
- Verification: targeted audit/import fault-injection tests, API/web builds, and lint pass. A browser test of committed data plus an injected screen-refresh failure is running. The freshness indicator itself, durable request idempotency for lost responses, and automatic retry of failed post-commit checks remain open.
- The first browser run reproduced a second bug: any failed background Setup query unmounted the whole editor, discarding the confirmation and drafts (`/tmp/mros-todo-import-browser.log`). Setup now retains previously loaded data/forms, explicitly warns they may be stale, and offers a refresh-only retry. First-load failures with no usable data still show the error state.
- Updated browser/database checkpoint `logs/e2e-20260913-191310.txt` passes 3/3: persisted inspection scheduling, real availability import with injected refresh failure, preserved unrelated directory draft, mobile containment, retry without duplicate submission, and technician/inspector workflow. Availability reconciliation and completion-override unit regressions are now included in `test.sh`, not just run manually. Full browser suite is running next.

### Full Browser Audit Follow-Up

- Unit drawer inspection/readiness calculation was still duplicated and ignored imported-ready vacancy statuses. It now uses the shared frontend helpers and labels readiness as "Unit ready" / "Yes - unit ready", not an assertion that an inspection occurred. Trade spelling variants now match final-walk eligibility. Pure contract tests pass; a production-image drawer rerun is pending.
- Full browser run reproduced an immediate-automation history failure: matching READY and DONE rules ran in unspecified database order; the latter could hide a forbidden READY attempt from the aggregate guard while both runs claimed success. Test-database run history confirmed READY followed by DONE. Added deterministic creation/id ordering and guard checks for each matched set-field action before applying the merged patch. This fix awaits browser rerun; the initiating user edit remains separately committed.
- Concurrent project-category initialization timed out during the same run's twelve-request test. No root cause has been confirmed; rerun without concurrent local builds and investigate further rather than simply increasing its timeout.
- The full 190-test browser run started before the drawer and automation follow-ups above. Its result will not be a final gate for those later changes.

### Subsequent Verification And Ready-Unit Panel

- The broad browser audit was stopped to repair reproduced failures: 48 passed, nine failed, one interrupted, and 132 not run. It is not a clean release gate.
- A focused rerun passed the automation guard, all seven pond interaction cases, and the technician/inspector workflow. Pond fixtures now explicitly reset inherited unit statuses and use controlled clocks for transient feeding assertions; no pond runtime timing was changed. Two failures remained: a drawer assertion targeted a focused-only element in the table drawer, and the concurrent category test timed out again.
- Fixed the drawer assertion to check the actual read-only whole-unit readiness field on desktop/mobile. Already-ready units now show a neutral final-walk notice without pending completion blockers or handoff actions; imported readiness does not imply an inspection or expose an inspector-only report. Five isolated final-walk tests pass, including the read-only API contract. The updated import/browser case passes; the broader rerun remains in progress.
- Compared twelve concurrent authenticated requests against the same disposable stack: IPv4 loopback returned all category requests in 72 ms and session requests in 49 ms, while localhost session requests hit the 20-second probe timeout. Database inspection showed no long-running category lock. The browser harness now uses explicit IPv4 loopback, matching its readiness probes, without extending test timeouts or changing production networking. A clean rerun is still required.
- IPv4 rerun passed all four focused cases (`logs/e2e-20260913-194228.txt`): saved import/refresh recovery and ready-unit drawer, concurrent category definitions, immediate automation readiness guards, and tech-to-inspector handoff/export. The category case completed in 1.4 seconds with its original concurrency and timeout. All 17 targeted status/receipt/work-view/inspection unit tests pass; the latest full API/build/lint gate is running next. No commit, push, deployment, or production data change in this session.
- Full isolated/API integration/build/lint/dependency gate passed at this checkpoint (`logs/test-20260913-194432.txt`). The complete browser suite is being rerun with the networking and fixture corrections; do not treat the earlier partial audit as a completed browser gate.
- The broad rerun found an obsolete status-alias test locator: it searches for a pill whose tooltip is the canonical storage value, while `LabelPill` deliberately uses the displayed alias for both text and tooltip. The captured page shows the correct alias in several vacancy cells and the raw-value snapshot check passed. Update the test to assert the user-visible pill and retain its independent stored-value checks after the current run finishes.
- Another warning-sprite fixture still copies the first three live test items without resetting their board groups. The captured pond has the scared and ready markers but excludes the sick fixture inherited from a down group. Apply the same explicit eligible-fixture setup used by the earlier pond tests; keep the actual down-unit exclusion and animated-pixel assertions intact.
- Resident-report limitation remains open in `docs/TURN_WORKFLOW_AUDIT.md`: the current PDF is deliberately a draft, not immutable signed issuance. Current-property branding and directory mailbox values are resolved during rendering. A final resident-issue feature must snapshot approved contents/branding/handoff details and verified sign-offs; readiness/import/override must not manufacture those records.

### Parts Draft Recovery In Progress

- Added an explicit conflict-review path for line-by-line additions. It reads the latest list without saving, preserves teammate changes, retains local additions, detects identical already-saved IDs after a lost response, rejects conflicting IDs/read-only turns, and enforces total capacity. Accepting the review updates the device draft; a separate save is still required.
- Nine isolated draft/recovery checks pass and lint passes. A dedicated mobile/desktop database-backed browser test covers teammate edits, reload after review, and a lost save response without duplicate rows; it has not run yet. The currently running full browser suite uses the preceding status-fix image, not this new parts UI. Updated production build and browser verification remain pending.
- The parts recovery browser test now passes against a separate disposable production-image stack: it preserves the teammate's order, restores the reviewed draft after mobile reload, requires an explicit save, and recognizes a committed response lost as 503 without duplicating the saved part. The initial test incorrectly tried to reopen a drawer already restored by mobile navigation; its locator was corrected, not the application's restoration behavior. Single-part edit conflicts remain a separate recovery follow-up.
- Reproduced the report reconnect risk in a real browser: a held background report refresh removes the inspection date and unsaved follow-up fields. The editor now remains mounted during transient fetch/failure/retry, while 401/403/404 responses still hide cached report contents. A browser rerun includes draft preservation, unchanged server draft, and access-revocation handling; verification is in progress.
- Both new browser cases pass (`logs/e2e-parts-report-20260913.txt`), including a lost-response parts save, explicit recovery after a teammate edit, retained report date/follow-up through transient refresh and retry, unchanged server draft, and hiding cached report fields on 403. Production images build and lint passes. The first post-fix report run used a brittle exact implicit-label locator for a populated textarea; the captured page retained both values, and a textbox-role locator completes the verification.
- The full preceding-image browser audit finished 185/190. Its five failures were the obsolete alias tooltip locator, three pond fixtures inheriting down/pending-inspection state, and the aggregate 36-screenshot test exceeding its single 45-second budget. Corrected the explicit fixtures and alias assertion; split visual capture into one test per display mode, retaining all nine workspaces and the original timeout per test. A ten-case follow-up rerun includes all corrections and both new mobile features. This is not yet a clean full-browser gate for the latest runtime.
- All ten follow-up browser cases pass (`logs/e2e-20260913-201059.txt`), including all four display-mode capture bundles, the three corrected pond fixtures, status alias/storage/permission checks, and both mobile recovery features. The latest full API/build/lint/database gate is running again for the complete local changeset. Broad status/issuance TODOs remain open; no push or deployment has occurred.
- Latest full API/build/lint/database/dependency gate passed (`logs/test-20260913-201338.txt`), including the parts and report-draft recovery changes. This checkpoint precedes further availability freshness work. The full-browser result remains the preceding 185/190 audit plus the successful ten-case follow-up, not a claim that all 195 current browser cases ran together.

### Availability Freshness In Progress

- Added a session-only, property-scoped read endpoint and a compact expandable board/Kanban/calendar indicator. It uses committed import receipts, separates report date from upload time, distinguishes full/partial/legacy-unknown coverage, and flags reports older than seven days, future dates, or unknown coverage for review without blocking work. A partial upload never claims the other units were refreshed. Missing receipts say no import is recorded, not that an import could never have happened.
- Three receipt/access tests pass and are wired into `test.sh`; production images build and lint passes. The browser import/parts/report-draft regressions pass with this feature present. Freshness browser coverage is rerunning after correcting a test that expected the expandable details to stay open through the board's property-switch reload; it also verifies current full-report display, old partial reports, no recorded import, failed polling/retry, scope isolation and mobile width.
- Freshness browser verification passes in `logs/e2e-20260913-203121.txt`: current full report, old partial report despite recent upload, missing history, property isolation, failed polling/retry, mobile width, and calendar presence. The remaining test-only navigation issue was using a desktop tab without opening the mobile View menu in a touch browser context. No production navigation changed.
- Existing-line materials recovery is now being verified: explicit latest-list review rebases only when the target line is unchanged, preserves unrelated teammate changes, detects an already-committed exact edit, and refuses to overwrite a changed/deleted target. Ten isolated draft checks cover these rules; database-backed mobile verification is next. Draft input is retained in memory even when browser storage fails.
- Full API/build/lint/database/dependency gate passed in `logs/test-20260913-203328.txt`, including freshness and existing-line materials recovery. This predates the photo-batch and final work-label follow-ups.
- Existing-line mobile recovery passed on a disposable stack and again in `logs/e2e-20260913-204027.txt`: teammate order preserved, review alone performs no write, explicit save succeeds, same-line conflict retains the local quantity and newer server quantity without overwriting either.
- Photo batches now show per-file server confirmations, device queue outcomes, failures and unattempted files. Validation failures no longer skip later selections; access/account changes stop further delivery. Failed files require explicit reselection, not automatic validation retries. ZIP help distinguishes server files from device-only files and explains Initial Walk stage filtering. Three isolated batch tests pass. The browser verifies a rejected middle photo, successful final photo, mobile containment and exactly two original-stage ZIP entries. Upload idempotency, retained failed-file recovery across reload and detailed queue-management improvements remain open.
- All four mobile recovery regressions pass in `logs/e2e-20260913-204027.txt`: freshness, batch/single-line materials, reconnect-safe report editing and photo/ZIP outcomes. A build caught `replaceAll` unsupported by the frontend's current TypeScript library target; changed to a compatible regex replacement without changing browser targets.
- Work-stage labels no longer infer active repairs merely from an unfinished field. Upcoming vacancy, archived, occupied and down/model turns have distinct labels; ordinary unfinished repairs say pending completion. Five status consistency tests pass, including the new cases. This is only a label correction, not the complete next-action/responsibility feature.
- Full API/build/lint/database/dependency gate passed in `logs/test-20260913-204332.txt`. A complete 197-case browser run is now in progress against the subsequent upload-outcome/status-label image; do not claim it has finished. Ambiguous HTTP/server or device-queue failures now say unconfirmed rather than claiming nothing was saved; four isolated batch tests pass, and the browser case now verifies a committed upload whose response was replaced with 503.
- Reproduced another shared-status discrepancy in the older least-loaded automation: three ready assignments counted as active, while an explicit pending final walk with stale completion YES was excluded. Active-load counting now uses operational readiness and retains the property/account candidate filters. The isolated regression passes; preview/execution database verification remains due in the next full API gate. This change is later than the currently running browser image. It does not modify guided percentage policies or existing assignments. Planned-block stale-status/cap semantics remain a separate audit item.
- Reproduced analytics counting repair DONE, cleaning DONE or archival alone as a completed turn. Completion-estimate eligibility now uses whole-unit readiness, including imported ready values and the pending-final-walk exception. Three analytics tests pass, retaining the future-date exclusion and untouched historical snapshots. API/JSON exports and the dashboard now disclose the legacy date-estimation basis; CSV methodology labeling remains a follow-up. This does not create actual inspection timestamps or rewrite past snapshots. Later than the running browser image; full verification is pending.
- Resident-code refresh audit: unlike the repaired report editor, the codes panel retained its cached/local form after 401/403/404. It now hides that form on access/not-found failures while preserving drafts through transient failures. Codes remain memory-only, not browser-persisted drafts. A focused mobile browser regression is running separately; this new change is not covered by the earlier full-run image.
- Build follow-up: the new assignment regression initially used `conditions: []`, accepted by its runtime path but rejected by the TypeScript `RuleConfig` contract. Corrected to `{ all: [] }`; all four analytics/assignment tests pass. The first claimed assignment build was not yet finished and ultimately failed on this fixture type. The corrected production-image build/browser run is in progress.
- Corrected API/web production images now build, lint passes, and the resident-code mobile test passes in `/tmp/mros-resident-code-browser-fixed.log`: transient refresh retains fictional unsaved input; a subsequent 403 removes the code form and show/save controls. No real codes were saved or exposed. Full API gate for these later analytics/assignment/code changes is still due after the main browser run.

### Backup Reliability Follow-Up

- [x] Reproduced false-success backup/restore wrappers with fake Docker commands: `set +e` around the logging pipeline also disabled failure stopping inside each worker. All seven backup/restore/prune workers now enable stop-on-error in their pipeline subshell and propagate both worker and log-write exit status. Four isolated tests cover failed configuration, failed database drop/create/restore, full-restore child failure, failed upload extraction, successful backup, and logging failure. Syntax checks pass. No real Docker backup/restore commands or production files were used by these tests.
- [ ] Coordinated restore remains incomplete: `restore-all.sh` calls a database restore that restarts the application before upload restoration clears/replaces its files. Design one coordinated maintenance window and preflight both artifacts before either destructive step; do not expose the application between database and upload restoration.
- [ ] Full-backup consistency and off-server protection remain open. Current scripts create separate database/upload files without an atomic manifest or verified paired restore, encryption, recovery-configuration coverage, or an off-server destination. Database retention does not cover upload archives. Verify snapshot consistency under active uploads/deletions, record checksums and restore results, and design paired retention before changing deletion policies.
- [x] Reproduced failed retention discovery deleting a partially enumerated fixture and reporting success. Pruning now enumerates into a private temporary manifest and checks discovery success before any backup deletion. On failure the manifest is removed and backups are left untouched. All five fake-command backup/restore/prune tests pass; normal dry-run/path protections remain covered by the next full gate.

### Complete Browser Checkpoint

- `logs/e2e-20260913-204658.txt`: all 197 browser cases passed in 20.6 minutes, including the status/import corrections, mobile batch and existing-line parts recovery, report-draft refresh/access handling, freshness, upload outcome/ZIP checks, desktop workflows, and all four visual modes. This supersedes the earlier incomplete/failed browser runs for that image.
- Later changes are not implied covered by that image: least-loaded assignment counts, analytics eligibility/basis disclosure, resident-code access-denial handling, and backup/restore/prune script fixes. They have isolated checks and the separate resident-code production-image browser pass; the next full API/build gate is still pending.
- Visual review found the expanded table filter controls extend into a horizontal strip, leaving the last filters off-screen despite no page-level overflow. Add a responsive wrapping layout and explicit per-control containment tests at 375, 768 and 1280px. The mobile filter panel should remain collapsed by default.
- Filter overflow was reproduced in a browser before the CSS fix. The expanded row now uses a responsive grid instead of horizontal scrolling; mobile collapse behavior is unchanged. Both latest-image filter containment and resident-code refresh/access tests pass in `/tmp/mros-filter-codes-final-browser.log`. Screenshots at 375 and 1280px were visually inspected; all dropdown choices are within the panel, with a taller explicitly expanded panel on phones.
- Latest full API/build/lint/database/dependency gate passed in `logs/test-20260913-211118.txt`, including analytics/readiness, least-loaded assignment preview/execution, code-panel build, filter layout, and five fake-command backup failure regressions. This is the current local gate. The 197-case browser pass plus the two later focused checks are distinct runs, not a claim that all 199 current cases ran together. No commit, push, deployment, or production data change has occurred in this work session.


## Current Follow-Up

Release preparation, 2026-09-09: feature/TODO work is paused at the user's request; only verification and deployment remain. Runtime checkpoint `a131108` passes the full API/build/lint/dependency gate (`logs/test-20260909-053149.txt`). Server backup `makereadyos-db-20260909-054342.dump` was restored into a network-isolated disposable local PostgreSQL container; all three new migrations applied successfully with `db:deploy`, and the expected column shapes were verified. The disposable database and local backup copy were removed; the verified database/upload archives remain on the server with rollback images. Existing unrelated live schema drift was not synchronized.

Browser release follow-up: full run `logs/e2e-20260909-053455.txt` finished 180/181. The new duplicate-map rejection fixture left duplicate names behind and broke a later native-backup test; a finally cleanup plus round-trip assertion fixes the test isolation, and both tests pass together (`logs/e2e-20260909-055306.txt`). The next rerun was stopped after a pond habitat test's real-time setup pushed its 72-second fast-forward beyond the snail's visit window (`logs/e2e-20260909-055433.txt`). The habitat test now pauses the clock after loading and advances until the intended visitors appear, freezing that visit while measuring both viewports. Pausing before loading was rejected because it also held loading callbacks. Pond runtime/timing is unchanged. All three affected tests pass three repetitions (`logs/e2e-20260909-060316.txt`, 9/9); the final full release gate follows. The earlier clean 176-test checkpoint below is not a clean gate for all subsequent changes.

Published native schema follow-up: all 65 accepted top-level data sections are now documented; 20 previously omitted sections made the published `additionalProperties: false` schema reject valid current exports. The guide no longer incorrectly says Projects are excluded. The section-key comparison passes, but permissive record definitions are not a replacement for the server's field/reference validation.

Local Pest save checkpoint (2026-09-09): detail PATCH now preserves omitted fields/status, rejects stale versions with 409, and keeps explicit unlinking intact. Only explicit unit selection discovers a new turn; unrelated edits no longer invent a close timestamp for historical closed records. Create/PATCH return a fresh issue after recurring flags update its version. A real database test also reproduced a stale NOT NULL constraint on `area` inherited from the original `location` column despite the nullable Prisma schema; migration `20260909102000_pest_optional_area` aligns storage without changing existing data. Requests still require a unit or area, including partial edits. Sixty isolated checks and two real-database/browser checks pass (`logs/e2e-20260909-052120.txt`); full API/build/lint/dependency gate passes (`logs/test-20260909-052311.txt`). Pest UI drafts and transactional recurrence/board/audit consistency remain open. Not pushed/deployed.

Latest local editor checkpoint: `46fde86`; full API/build/lint/dependency checks pass in `logs/test-20260909-044735.txt`. Full browser run `logs/e2e-20260909-045025.txt` finished 177/179: a calendar interception fetched a response across reload/disposal, and a pond test allowed only five seconds despite authored sleepy-frog rests of about 9.5 seconds. The calendar now uses a captured fixture and stable ID; the pond test allows a complete rest while still requiring real displacement. Both plus the mobile Lease draft flow pass three consecutive repetitions (`logs/e2e-20260909-050914.txt`, 9/9). No application animation timing was changed. This focused verification is not a new clean full-browser gate. Production/origin remain `0ec90ff`; later changes are local.

Latest verified local checkpoint: `2d0d658`. All 176 browser checks passed in `logs/e2e-20260909-035602.txt`; full API/build/lint/dependency checks passed in `logs/test-20260909-035329.txt`. This includes the later Project/Pest/PM scope and reference fixes and property-scoped operational staff pickers. Production and origin remain `0ec90ff`; subsequent commits are not pushed or deployed. Earlier checkpoints below are historical, not current release status.

2026-09-09 full browser checkpoint: all 170 tests pass in `logs/e2e-20260909-024418.txt` at runtime commit `10424dc`, including technician/inspector handoff, one-page draft PDF, initial-walk evidence, project scope, native-import outcomes, offline ownership, desktop and mobile flows. This supersedes the earlier full-browser failure/pending notes for those runtime changes. Production remains `0ec90ff`; later commits have not been pushed or deployed. Last full API/build gate `logs/test-20260909-023744.txt` predates the small project-scope patch, whose isolated tests and API build pass; another full API gate is next.

Follow-up full gate: `logs/test-20260909-025947.txt` passes at the same runtime checkpoint, including project access tests, API integration, builds, lint and dependency audits. Published native-backup and library-pack schemas now document optional nullable status display names; both JSON files parse and bounded field definitions were checked. No later deployment has occurred.

- Local sprite playback correction (2026-09-07): live inspection confirmed sprite coordinates were advancing, but each frog repeated one seeded short run indefinitely, sometimes mostly identical idle frames. The new player rotates authored idle/croak/hop/jump clips with consistent full-sheet direction rows, separate compact accessory action rows, complete five-frame compact jumps/hops and full-sheet worry clips. Sleeping poses remain slower; no global motion preference was overridden. Three production-image browser regressions passed (`logs/e2e-20260907-103047.txt`), including pixel/nonempty-tile checks across all 11 sheets, actual background-position and action changes, pause/resume, reduced motion, unit opening, feeding and outfit persistence. API/web builds and lint passed. Not pushed or deployed; the user's device-specific static behavior was not reproduced in the live browser check.

- 2026-09-07: user approved moving live VAB 2907P's repair start from August 26 to September 8. Updated only `turnMaintenanceDate` through the audited custom-field API and verified the September 8 event after a fresh login. Vacated date, expected finish, move-in, completion and assignment were unchanged. Expected finish remains September 5; it was not silently shifted.
- Local past-due-starts list: shows unfinished starts before today, including previous months, above the calendars. Keeps original dates; honors existing filters and property access; excludes archived, completed, ready and occupied units. Entries open the unit drawer and remain visible when navigating months. API/web builds, lint and four calendar browser regressions passed (`logs/e2e-20260907-093230.txt`), including mobile containment and completed-item removal. The earlier browser run used a desktop navigation locator after switching to mobile; the final corrected run passed. This UI change is not pushed or deployed yet.
- [ ] Rescheduling consistency: flag an expected finish or downstream stage before a saved repair start. Offer an explicit preview of downstream date changes, preserving manual dates unless confirmed; never silently treat an imported finish date as a repair start.

Production start-calendar checkpoint (2026-09-07): deployed `d1fd2d5` to `mr-os.com`. Verified predeploy database backup `makereadyos-db-20260907-091538.dump` and upload archive `makereadyos-uploads-20260907-091539.tgz`; previous API/web images retained as `rollback-7ede744`. Both services healthy, with no pending migrations. Public login and read-only calendar checks confirmed 20 NTV units with projected starts, including six displayed on their expected dates in September. Mobile containment and uncaught-error checks passed. No business records were changed during verification. Local-only notes below describe the predeployment checkpoint.

Start-calendar planning correction (2026-09-07, local only): notice-to-vacate units now receive a read-time projected start from `moveOutDate` (NTV / Expected Vacate), on the next weekday respecting property Monday/Friday restrictions. Actual vacant turns prefer `vacatedDate`. Saved start dates take precedence; forecasts never write dates, assign staff or mark work complete. Projections recompute after expected dates change and ignore prior actual-vacate dates on NTV units. Ready, occupied, completed, archived and undated units are not projected. Calendar events show property/unit and a Projected label, retain explicit filters/property permissions, and bypass board window pagination. Browser regression passed (`logs/e2e-20260907-091045.txt`, 3/3): two properties' unassigned notices, no persisted start or assignment, windowed-board bypass, preserved saved-day placement in Central time and separate finish track. Full validation checkpoint follows after completion. No migration or live data changes.

Start projection verification: full API/integration/build/lint/audit gate passed (`logs/test-20260907-091225.txt`). Earlier attempts caught a fixture code-length error, missing test-only environment settings and a concurrent local Docker port collision; the final sequential run is authoritative. No code changes after the successful browser run other than this verification note. Not pushed or deployed.

Branding foundation and rollout: see [PROPERTY_BRANDING.md](PROPERTY_BRANDING.md). Each property explicitly selects a reusable management company; no default assignment. Admin company/property logos and native backup support are implemented locally. Cross-module report/header/email integrations remain explicit TODOs rather than implied completed coverage. The expanded two-page final-walk sample remains fictional and unconnected to finalization.

- [ ] Admin Account > View as user: provide a selected user's real role/property/My Work view, not merely hide admin buttons. Proposed default is read-only; user preference requested. Always show preview identity and Return to admin. Enforce original active admin/session authorization server-side on every request; block writes, password/invite/account actions, notification-read side effects and offline delivery in preview. Keep admin actor/effective user distinct in audit records. Isolate or clear all React Query/service-worker/offline caches on entry/exit; do not reuse the target user's password or change their session. Test non-admin rejection, revoked admin/target access, property scope, attachments/exports, concurrent tabs, session expiry, refresh/exit and no business-record mutation. This feature is not implemented yet.

Local automatic-baseline follow-up (2026-09-07, not deployed): new properties receive the five-working-day scheduling pack in the property-creation transaction. Seed provisions active properties without a guided pack; the five-minute scheduler also reconciles properties created through other import paths before executing rules. Disabled, archived, partial and customized guided packs are left alone. Existing dates remain unchanged; eligibility protections still exclude ready, occupied, NTV, archived and completed units. The guide is optional customization/pause/resume, not required activation. No staff assignments, vendor bookings or completed inspections are inferred. Full integration/build checks passed (`logs/test-20260907-081908.txt`); three focused browser checks passed (`logs/e2e-20260907-082206.txt`), including fresh-property enabled defaults and date filling through the existing executor without an enable call. The timer cadence and repeat/paused/customized provisioning are covered by focused unit tests. Conflicting archived/mistyped global date fields require manager correction rather than silent replacement.

Latest deployment checkpoint, 2026-09-07: start-calendar fixes `fb09e2d` and bootstrap-account preservation `d55c2f6` are live. With explicit user approval, five-working-day schedules are enabled for TA and VAB; 12 missing dates were filled across three eligible units without replacing existing dates or assignments. Ready/occupied/NTV units are excluded; two further units need Vacated dates. Full API/integration/build and four focused browser checks passed, as did both code CI runs and live login/mobile smoke checks. Original admin settings were restored from backup after discovering the old seed overwrite and verified unchanged after restarting the corrected API. See END_USER_UX_AUDIT.md for dates, backups and test logs. Earlier local-only notes are historical.

Active end-user interaction/ease-of-use audit: see [END_USER_UX_AUDIT.md](END_USER_UX_AUDIT.md) for prioritized findings, completion evidence, and remaining UX work. Work in this audit is local only; the latest deployment checkpoint remains authoritative for production coverage.

Local follow-up: separate **Make Ready (Start)** from **Expected Finish**. The seed provisions the existing managed repair-start DATE field/track even before guided scheduling is enabled; only recognized legacy labels are renamed, preserving track IDs, custom names, archives, and dates. The old `makeReadyDate` remains the finish deadline in both board labels and calendars. Fresh Schedule views show start and move-in side by side; Expected Finish is independently selectable, and saved selections remain supported. Multi-day repair plans now anchor the repair start to the first eligible weekday, while later targets retain cumulative duration offsets. No existing dates are copied or rescheduled, and move-in deadline enforcement remains future work. Full API/PDF checks passed (`logs/test-20260906-234333.txt`); lint and final production-image builds passed. Guided dates, display modes, and custom date tracks passed in `logs/e2e-20260906-234830.txt`; after correcting a new test's seeded-unit lookup, the default start/finish separation and mobile-width check passed (`logs/e2e-20260906-234953.txt`). Not pushed or deployed.

Deployed code release `8f086b5ebe3d5ac1a3a27a27ae4b5855523983e4` to `mr-os.com` on 2026-09-06. The additive assignment-policy migration applied successfully via `db:deploy`; startup reported no pending migrations and no schema-sync fallback ran. Database/API health and public HTTP checks passed. A real public-browser session using the configured admin credentials verified login, Frog Pond settings/selection and unit drawer, and both scheduling/assignment guides, with no uncaught browser errors. No scheduling or assignment policies were enabled by the deployment. The previously supplied account password was not accepted in the first browser smoke attempt; no account credentials were changed. Final code CI run `34082988465` passed. Backups and `rollback-b7d6f67` images remain available; Docker, MakeReadyOS, and Cloudflare boot services remain enabled. The previously documented live-schema differences remain untouched. Earlier "not deployed" notes below are historical checkpoints.

Release browser gate: 84 of 85 full-suite checks passed (`logs/e2e-20260906-231421.txt`). The remaining pool test exposed a real cross-tab form reset race: React reused the facility form as the chemical form while the earlier save was still refreshing data. Pool daily/facility/chemical forms now have distinct property-scoped keys, so a completed save resets only its original form. The regression asserts the old form is detached after switching tabs. Lint, rebuilt production images, and all five focused pool/mobile/pond checks passed (`logs/e2e-20260906-232300.txt`). GitHub CI for the main release `063be13` passed. This final UI correction does not change the API or migration; live cutover is next.

Release preparation, 2026-09-06: full API/PDF integration and production builds passed (`logs/test-20260906-231153.txt`). Full browser verification is running before live cutover. Server database backup `makereadyos-db-20260906-231245.dump` and uploads backup `makereadyos-uploads-20260906-231312.tgz` were created and their archive contents verified. Existing API/web images are retained as `rollback-b7d6f67`. The only new migration adds `TurnAssignmentPolicy`; it does not rewrite existing board data. Guided scheduling and assignment require explicit per-property activation. This preparation note does not mean deployment has completed.

Frog Pond visual and interaction polish is verified locally. Existing pixel-art scenery now uses proportional cropping, lily pads, ripples, ambient light, and body-only staggered hops; marker anchors no longer evade the pointer. Settings collapse behind one disclosure, motion has a direct pause control, hidden tabs stop animation timers, and reduced-motion preferences are honored. Explicit arrange mode separates dragging from normal mobile scrolling. Responsive spacing caps the mobile scene at three rows and exposes remaining units through group links; screenshot inspection and a normal-click regression caught and corrected overlapping mobile targets. Accessory descriptions now report real work status rather than invented awards. Lint, production-image build, and three focused browser checks passed (`logs/e2e-20260906-230913.txt`); desktop/mobile screenshots were inspected. No new dependencies. Not pushed or deployed; full browser/API suites and physical-phone drag testing were not rerun for this UI-only change.

Connection-banner correction is verified locally. A single rejected fetch previously left the API-degraded flag set until manual retry or a browser online event; successful requests never cleared it, and cancelled requests could trigger it. Abort errors now remain cancellations. While degraded and online, a bounded, serial, uncached `/auth/me` probe verifies a JSON identity response before clearing the warning, retries failures, cancels on cleanup, and ignores recovery superseded by a newer failure. Retry/online no longer blindly dismiss API degradation. Offline-queue warnings and failed-save errors remain separate. Four focused regressions, lint, production-image builds, and two browser checks passed (`logs/e2e-20260906-225344.txt`). `e2e.sh` now forwards optional Playwright arguments for focused checks. Public `mr-os.com/api/meta` returned the expected unauthenticated JSON 401 during spot checks; this does not establish the cause of the original failed request or long-term network stability. Not pushed or deployed. The full API suite was not rerun for this frontend-only correction.

Next workflow milestone: replace the user's two-page manual make-ready sheet with one staged digital turn and a source-backed resident handoff. Both source pages were reviewed; the required sequence, checklist coverage, independent leasing final walk, rework, privacy, and release gates are captured in `docs/DIGITAL_TURN_WORKFLOW.md`. This larger workflow is not implemented yet. An immediate wording correction removes the unsupported "passed final walk" claim from the existing mark-ready audit/notification and removes "Manager Signoff" from the button; the action still only marks the unit ready. Production builds, lint, and full API/PDF integration passed (`logs/test-20260906-224613.txt`), including a new real-database regression for honest ready-status audit descriptions. Not pushed or deployed.

Property percentage turn assignment is implemented locally alongside the calendar guide. Configure independent 100% splits per property (for example, VAB manager 100%; TA manager 25% / tech 75%). Persistent weighted rotation preserves existing assignments and its place across repeated saves/restarts, validates staff property access, pauses safely, and creates preference-aware assignment alerts. A new migration stores policies and balancing credits. All 84 browser checks passed (`logs/e2e-20260906-223208.txt`), including 25/75 versus 100% allocation, partial-cycle persistence, concurrent runs, exclusions, revoked access, pause, and notification delivery. Full API/PDF integration, production builds, lint, and dependency audits passed (`logs/test-20260906-223916.txt`). Mobile guide visually reviewed at 412px. Not pushed or deployed. See `docs/TURN_SCHEDULING.md` for limits and native-transfer follow-up.

Guided turn scheduling is implemented and verified locally; see `docs/TURN_SCHEDULING.md`. It replaces a rule-library-first entry with an enabled five-stage weekday pack, creates calendar tracks, preserves existing dates, and provides app-hosted checks plus pause/retry. Calendar now opens to the current month instead of May 2026. All 83 browser checks passed (`logs/e2e-20260906-220922.txt`), including mobile layout, setup retry, concurrent setup, calendar population, and pause. Final API/PDF integration and production builds passed (`logs/test-20260906-221657.txt`). This batch is not pushed or deployed.

Release baseline: `176474f` (2026-09-06), including the earlier reliability release `da50084` and the one-additional-user setup check. The follow-up items below are the next release batch; per-item "Local only" notes describe their original verification checkpoints, not deployment status. Full API/PDF checks passed through `logs/test-20260906-212816.txt`, including metadata, lease assignment, and conservative occupancy import changes. All 82 final production-image browser checks passed (`logs/e2e-20260906-213300.txt`). Database and uploads were backed up on the server at 21:33 CDT; rollback images retain baseline `176474f`. Deployment health checks remain the final release gate. Historical checkpoint notes below are retained for audit context.

- [x] Condense refrigerant exports from six rows and 24 links to one report selector, one format selector, and one generate action. Full report/PDF is the default; focused legacy reports remain available without removing data. Show property scope, all-date coverage, and shared/current inventory disclosure. Desktop/mobile-width browser regression and web build passed. Local only.
- [ ] Extend the consolidated report builder with date/unit filters and selectable sections backed by actual API/export coverage. Do not imply these filters exist before implementing and testing them across PDF, spreadsheet, and CSV.
- [ ] Replace legacy refrigerant "Excel" exports with genuine XLSX workbooks (or clearly label and name TSV output). `/refrigerant/export.xls` currently returns tab-delimited text under an XLS filename/MIME type, not an Excel workbook. Preserve formula safety, leading-zero unit numbers, section coverage, and readable column sizing in the replacement.
- [ ] Filter board staff pickers to the selected item's properties. Current StaffOption omits property memberships, so the server now correctly rejects out-of-scope selections but the picker can still offer them. Add a minimal scoped eligibility response rather than exposing full user records; preserve existing historical name labels during ID migration.
- [x] Tighten metadata access: staff summaries require shared property access (admins remain available), and automation summaries are manager/admin-only, property-scoped, and limited to their declared public fields. Focused no-access/scoped/admin fixtures and full API/PDF integration passed (`logs/test-20260906-212002.txt`). Do not treat earlier unrestricted metadata exposure as proof of misuse. Local only.
- [x] Validate lease assignees: create/edit require an active editing-capable user with property access (or an admin), rejecting unavailable IDs before issue writes. Focused route tests and full API/PDF integration passed (`logs/test-20260906-212511.txt`). Local only.
- [x] Verify conservative occupancy defaults: unknown or blank imported status text now maps to UNKNOWN instead of OCCUPIED; new directory units with omitted status also use UNKNOWN. Existing unit status remains unchanged when omitted. Pure fixtures and fresh-database create/reimport checks passed (`logs/test-20260906-212816.txt`).
- [ ] Review RealPage XML inference separately, especially omitted vacancy flags, before assuming it establishes physical occupancy. Show unknown-status coverage beside occupancy percentages so missing classifications are not presented as a definitive occupied/vacant split.
- [ ] Validate lease issue foreign references against the issue's property on create/edit: unit, issue type, and property map IDs should not silently link another property's records. Include cleared/unchanged/archived references and import compatibility in tests.

Lease-reference reproduction (2026-09-09, local): the actual compiled PATCH route with a scoped-manager request and stubbed Prisma lookup accepted arbitrary unit/type/map IDs into its update statement without resolving their property. The update stub threw `AUDIT_STOP_BEFORE_WRITE`; no database reads or writes occurred. Add create/update tests rejecting missing or wrong-property references, reject newly selected inactive/archived records, permit unchanged valid historical references and explicit clearing, and preserve a unit/building/area location after partial edits. Changing an issue's property should be explicitly rejected rather than silently ignored. Native backup links need a separate property-consistency check before calling this protection complete.

Local reference-guard checkpoint: 24 new route subtests first reproduced missing validation, then passed. New foreign/missing/inactive unit/type/map selections fail before setup or issue writes; unchanged same-property inactive links remain editable, and explicit nulls can clear bad historical links. Partial location edits cannot clear the last location, and changing property is explicitly rejected. Native imports now reject foreign issue-type keys and missing units, resolve omitted-but-existing same-property references, and roll back if required references cannot be resolved at write time. This does not repair old foreign links automatically or solve concurrent per-keystroke edits. Real-database/browser verification is in progress.

Reference-guard verification: all four focused browser checks passed in `logs/e2e-20260909-021954.txt`, including separate technician/leasing sessions, real cross-property rejection, historical inactive-link retention, rejected foreign-key restore, and a partial native restore that retains unit/type/map links without their directory rows in the file. Existing paginated Quick Capture and grounds workflows also pass. Full API/build/lint/audit gate passed in `logs/test-20260909-022107.txt`, including 26 assignment/reference tests. The broad concurrent-edit and historical repair items remain open. Local only; full browser rerun still pending.

- [x] Replace Lease detail-field per-keystroke saves with explicit Save/Discard and account/property/issue-scoped device drafts. Unit, building, area, status, priority, assignee and notice stage edits submit together with their original version and expected-account header. Failed saves retain inputs; reopening/reloading offers Resume/Discard. Refresh retains the draft, shows latest-versus-draft values after a conflict, and requires explicit confirmation before rebasing. Storage failures warn without discarding in-memory edits; an online save remains possible. Three parser tests and mobile browser failure/reload/conflict/storage cases pass, together with actual TECH/LEASING/ADMIN picker saves and dashboard regressions (`logs/e2e-20260909-044538.txt`, 4/4). Mobile editor screenshot reviewed with no overflow. Local drafts are not team records, backups or automatically queued saves. Local, not deployed.
- [x] Remove action buttons nested inside Lease's expand/collapse button. The mobile draft test reproduced header clicks advancing notice stages instead of collapsing. Notice/resolve actions are now sibling buttons; the header exposes its expanded state. Regression verifies no nested buttons, real collapse/reopen and an unchanged edit version through that interaction (`logs/e2e-20260909-044308.txt`, 5/5 focused checks).
- [ ] Extend recoverable saves to Lease notes, persistence, notices, resolution, archive and photos, plus Pest detail editing. Those separate actions still have their own pending/error semantics. Coordinate device drafts across tabs, handle archive/access revocation while a draft is open, and add browser recovery controls for orphaned drafts rather than claiming local storage is durable backup.

Local Lease PATCH safeguard: omitted fields are no longer copied from a stale snapshot into the update; explicit nulls still clear fields. Optional `expectedUpdatedAt` rejects stale clients, and the conditional database update checks the validated record timestamp even without that client field. Conflicts return 409; recurrence processing uses the updated unit. This does not make related-directory changes, recurrence/audit writes, or all other mutation routes atomic. Four regressions reproduced stale overwrites/ignored versions/generic errors; 31 isolated checks now pass. Real PostgreSQL tests verify unchanged unrelated fields, explicit stale rejection, reviewed retry, and competing location clears where only one succeeds (`logs/e2e-20260909-042933.txt`, 4/4 focused checks). The prior totals browser fixture depended on metadata timing after API-only setup; an explicit reload removes that race. Full API checks for totals passed in `logs/test-20260909-042003.txt`; the subsequent PATCH safeguard gate is next. Local, not deployed.
- [x] Align direct project user-assignee validation with the picker: shared eligible roles exclude viewers, alongside active/property checks. Existing historical assignments are not rewritten. Route tests cover record/task create/edit rejection plus allowed roles; real scoped-manager assignment checks pass in `logs/e2e-20260909-024246.txt`. Cross-module role/actionability review remains open below. Local only.
- [ ] Decide and implement actionable project-task permissions for every offered assignee role. The picker includes leasing (project read-only) and cleaners (no Projects module access), so assignment eligibility alone does not establish that recipients can complete their task. Provide narrow assigned-task completion where intended, or stop offering non-actionable recipients with a clear explanation; test My Work visibility and completion without granting broad project-edit access. Keep this separate from make-ready final-walk permissions.
- [x] Require board assignments to cover the target properties. Creation, single edits, and bulk assignment require one active staff record covering every target property (admins remain globally eligible). Focused rejection regressions and full API/PDF integration passed (`logs/test-20260906-205516.txt`). Name-based ambiguity and stable user-ID migration remain separate. Local only.
- [x] Scope assignment notification recipients by property and skip ambiguous eligible namesakes instead of picking the first record. Focused tests and full API/PDF integration passed (`logs/test-20260906-210507.txt`). Local only.
- [x] Filter notification lists, unread totals, and preferences by current property access, retaining global notices and unrestricted admin visibility. Focused scoped/no-access/admin fixtures and full API/PDF integration passed (`logs/test-20260906-210507.txt`). Historical notifications are hidden, not deleted. Local only.
- [x] Exclude three optional setup recommendations (templates, starter automations, dashboard review) from required progress. Keep their actions available and label them optional. Focused browser check confirms five required admin checks rather than eight; build passed. Local only.
- [x] Normalize readiness/occupancy separators: API and UI handle spaced, underscore, and hyphen variants consistently, and both NTV variants remain occupied. Shared fixtures and real-database import/section assertions passed in full API/PDF integration (`logs/test-20260906-211116.txt`). Legacy VACANT_LEASED section semantics remain unchanged pending a separate decision. Local only.
- [ ] Finish canonical status mapping beyond separator handling. Section selection still treats legacy VACANT_LEASED as ready; resolve that legacy meaning explicitly. Audit custom statuses and all other import/preview paths before replacing their mappings wholesale.

## P0: Trust And Safe Saves

- [x] Close the shared project mutation property-scope gap. The helper now checks property scope before every role shortcut, including restricted admin tokens. Record edits, conversion, comments, tasks, attachments and wiki-reference writes share the check; PATCH rejects a changed property ID with 409 instead of orphaning linked scopes. Forty-one isolated route assertions pass after first reproducing manager/token bypasses and viewer assignment. Three real-browser checks pass in `logs/e2e-20260909-024246.txt`, including unchanged out-of-scope records/tasks and permitted in-scope edits. API build passes. Local only; full browser gate next.
- [ ] Validate project category/map/wiki references independently of parent-record authorization. A caller may have access to its own project but supply a foreign property's related ID. Include global categories, inactive/archived history, clears, exports and native imports; do not silently rewrite historical links.

Local direct-route checkpoint: project create/edit validates category/map scope and new-link active state; global categories and unchanged in-scope historical references remain supported. New wiki links require an active target from the same property. Category clears reset the saved category name; map clears reset pins, and a changed map without supplied coordinates starts unpinned. Existing foreign links are not silently retained on edit; an explicit clear can repair them. Seventy-eight access/reference assertions and API build pass. Four real-browser/database checks pass in `logs/e2e-20260909-030543.txt`, including retained historical links, denied new inactive links and cleared details absent from HTML/native export. This is not the native-import repair below, nor atomic reference/archive concurrency protection. Local only; the prior 170-test full gate predates this focused patch.
- [ ] Make project capture property changes explicit and safe for existing drafts. Current property effect only replaces `draft.propertyId`, retaining the old category, map/pin, staff, source references, text and photos. Preserve field work without silently reattributing evidence: confirm/review property switches, clear or revalidate scoped links, associate pending requests with their original property, and block conflicting switches while a save is unresolved. Map changes also need to clear the old draft coordinates rather than sending them as an intentional new pin.

Local map-selection checkpoint: selecting or clearing a capture map now clears its old coordinates while preserving other draft content. Explicit accessible label/description keeps the helper text separate from the control name. Four production-image browser tests pass in `logs/e2e-20260909-032209.txt`: old pin disappears after switching maps, remains absent when switching back, saved coordinates are null, delayed-property capture works and retained photo previews remain valid. The red regression first reproduced a carried pin (`logs/e2e-20260909-031735.txt`); an intermediate help-label regression was fixed without weakening the accessible-name assertion. Property-switch draft ownership remains open. Local only.

- [x] Reject PM template property moves through ordinary PATCH with 409. Seven route assertions pass after reproducing manager/admin writes before the fix. Four production-image checks pass in `logs/e2e-20260909-032609.txt`, including same-property manager edits and unchanged generated task IDs/ownership after rejected manager/admin moves. A transfer workflow is not implemented. Local only.
- [x] Revalidate retained PM assignees when the assigned role changes. Role-only changes that conflict with the existing user now return 400 before writes; explicit clear and compatible replacement remain supported. Ordinary edits do not erase inactive assignment history. Eleven PM route assertions pass after reproducing the role-only bypass. Four production-image checks pass in `logs/e2e-20260909-033928.txt`, including unchanged generated-task role/user after rejection and successful explicit reassignment/clear. Local only.
- [ ] Make PM template edits and generated-task synchronization atomic. The template update, pending-task update, new-task generation, audit and webhook steps currently execute separately; a later failure can report an error after the template changed but tasks did not. Add database fault-injection and concurrent-edit coverage, then separate committed mutation outcomes from post-commit delivery failures. The property/assignment guards do not provide this guarantee.
- [ ] Validate Pest issue unit/vendor/staff references on create/edit and enforce that an explicitly linked make-ready belongs to the selected unit as well as the property. Current linked-turn lookup checks the turn's property but not unit agreement, and optional unit IDs without a matching turn do not establish directory scope. Include unchanged/cleared/inactive references and native-import behavior; do not silently rewrite old evidence or assignments.

Local direct-route checkpoint: Pest create/edit now validates unit/vendor property scope, assignee property access and new-link active/eligible state. Explicit linked turns must match the selected unit and cannot be newly linked when archived. Unchanged valid historical inactive/archived links remain supported. New assignees match Pest edit roles (admin/manager/tech/leasing); picker no longer offers viewers. A changed property ID returns 409 rather than being silently ignored. Thirty-eight route assertions and API build pass. Five production-image checks pass in `logs/e2e-20260909-033241.txt`, including separate tech/leasing sessions, wrong unit/vendor/staff/turn rejection, retained inactive vendor history, photo retry and board-to-Pest launch. Native-import repair, explicit unlink/auto-link semantics, property/unit evidence reassignment and per-keystroke save races remain open. Local only.
- [ ] Audit native project child ownership and partial restores: comments/tasks/attachments/wiki references currently trust each supplied `propertyCode` separately from their `recordKey`. Preflight checks existence, not matching parent ownership. Project category/map references accepted from existing database directories can then become null during the write phase because only incoming-directory maps are consulted. Wiki fallback preflight lookup is unscoped and not mirrored by write resolution. Add mismatched-child rejection and existing-directory round-trip fixtures before claiming a complete restore.

Local child-ownership checkpoint: native preflight now rejects project comments, tasks, attachments and wiki references whose property disagrees with the incoming parent project. Imported wiki targets must also match the reference property. A real database red test accepted a foreign-property comment before the fix (`logs/e2e-20260909-030831.txt`); all four focused checks pass afterward (`logs/e2e-20260909-030942.txt`), covering every child type, wrong wiki target, no project created on rejection, and a successful scoped import. Existing-directory resolution and exact category identity remain open; this does not repair historical bad links or validate every other native-backup module. Local only.

Local partial-map restore checkpoint: the real database regression first imported a project with retained pin coordinates but a null map (`logs/e2e-20260909-052626.txt`). Project restores now resolve an existing map within the record's property even when map/property directories are omitted, preserve scoped child comments, and reject duplicate same-property map names instead of guessing. Three browser checks pass (`logs/e2e-20260909-052851.txt`), including archived map history and rejection of a map found only at another property. Ten isolated backup assertions pass, including fault injection where property/map disappears or map identity becomes ambiguous between preflight and the transaction; these are stubbed transaction checks, not database fault injection. Full API/build/lint/dependency gate passes (`logs/test-20260909-053149.txt`). Category and wiki-target resolution remain open, as do map identity/ambiguity in other native-backup sections. No historical links were rewritten. Local, not deployed.

Full API/integration/build/lint/dependency gate passes for direct project references and native child ownership in `logs/test-20260909-031140.txt` at `adca040`. Focused browser results are above; the clean 170-test full browser checkpoint predates these two patches.
- [ ] Preserve exact project category identity in native backups. Project records export only the saved category name, not whether the selected category was global or property-scoped; identical names in those scopes can restore to the wrong category. Add a backward-compatible scoped category key, reject ambiguous legacy matches rather than guessing, and cover renamed/deleted historical category labels separately from linked identities.

- [x] Distinguish native-import preview, preflight rejection, committed merge and refresh failure. The API returns `applied`; import audit is inside the merge transaction. The UI retains committed summaries, warns honestly about retained conflicts, locks file/import/export actions while pending, and offers refresh-only retry without repeating the import. Five isolated assertions cover outcome/audit placement. Real database and browser checks pass in `logs/e2e-20260909-023635.txt`: rejected preflight writes nothing, preview writes nothing, a merge creates a property while retaining conflicting shared settings, and a failed UI refresh preserves the confirmed result. The audit-failure test uses transaction stubs, not database fault injection. Local only; full gates pending.
- [ ] Add durable native-import operation receipts/idempotency for a lost response. Explicit confirmed outcomes do not resolve an interrupted response after commit; users must inspect current data before retrying. Large-import timeout/load behavior also needs separate coverage.

Native-import checkpoint: full isolated/API integration, lint, builds and dependency audits pass in `logs/test-20260909-023744.txt`. Focused browser verification is listed above; a clean full browser rerun remains required. No production data was used or modified.

- [ ] Prevent duplicate global project-category initialization and make native backup round trips deterministic. Full browser run at `0a989a4` exposed 19 duplicate global default categories after Projects was visited. `ensureDefaultCategories` uses count-then-create; PostgreSQL's nullable `(propertyId, name)` uniqueness does not prevent duplicate names when propertyId is null. Serialize initialization and global category create/rename paths, add real concurrent-request coverage, and define safe treatment of already duplicated categories without silently merging conflicting settings or losing project links. Native export currently emits duplicate portable keys and import then rejects its own export. Keep this separate from legitimate same-named categories in different properties.

Local prevention checkpoint: default initialization rechecks under a shared transaction lock; explicit create/rename and native-import category creation participate in the same lock. Duplicate names within a scope, including inactive categories, return 409. Existing category property scope cannot be moved; missing edit targets return 404. A deterministic stub reproduction produced two batches/38 rows for 19 names before the fix. Five real-browser checks pass in `logs/e2e-20260909-021320.txt`, including twelve simultaneous initial reads, competing creates, name collisions, separate-property names and native preview. Calendar fixture IDs are now captured before locators are built, and pond assertions verify distinct positions/non-grid distribution rather than requiring every x-coordinate to differ. Existing duplicate categories are NOT merged/deleted; a safe legacy repair/portable-backup policy remains open. Full-suite rerun remains pending; local only.

- [x] Restrict `/planning/capacities` to explicit public staff fields rather than returning full Prisma user records (including credential hashes). Filter capacity visibility by staff scope and restrict global capacity edits to managers covering all of the staff member's properties, or admins. Isolated tests cover credential omission, cross-property rejection, and permitted edits. Deployed in the reliability release; review exposure history and credential-rotation needs without assuming misuse occurred.
- [x] Restrict map-pin creator/editor relations to ID, display name, and role. The shared include previously returned complete user records through pin responses. A regression verifies password and reset hashes are excluded. Include this endpoint in the exposure review above.
- [x] Stop new pool logs from silently passing omitted observations: safety selectors default to Not checked, missing standard checks are preserved as NOT_CHECKED, missing evaluated chemistry produces INCOMPLETE, and an explicit failed safety check produces REVIEW. Partial logs remain saveable; review totals include incomplete entries. API fixtures and mobile save/history tests passed. Historical records are not rewritten; this is data-completeness labeling, not certification of regulatory compliance. Local only; not deployed.
- [x] Reject duplicate pool safety labels (including surrounding whitespace) before saving; a repeated PASS must not overwrite an earlier FAIL. The route regression first reproduced the overwrite, then passed with schema validation. Local only; not deployed.
- [x] Require an explicit pool property for saving logs, facilities, and chemicals. All-properties mode now explains the requirement and disables save buttons instead of silently ignoring submissions. Focused browser checks passed; clean-install fixture explicitly selects its property. Local only; not deployed.
- [ ] Validate product-specific pool dosing, units, concentration basis, and operator-selected chemical separately from generic correction labels. Review optional reading schedules, boolean operational observations, legacy PASS defaults in storage/import paths, and historical entries before claiming complete inspection coverage.
- [x] Checkpoint confirmed offline progress: persist project/lease/pest record IDs before attachments, remove each confirmed upload from the durable remaining-file list, and persist delivery completion before queue deletion. Thirteen orchestration regressions include first/second-photo failures, fresh-module retry, and failed queue cleanup. Local only; not deployed.
- [x] Verify confirmed photo progress using actual browser IndexedDB and page reload: a failed second lease photo remains queued, confirmed creation and first photo are not repeated, and retry clears the queue. This is separate from server idempotency. Local only; not deployed.
- [ ] Add server idempotency for ambiguous offline responses and cross-tab delivery. Confirmed-progress checkpoints cannot prevent duplicates when a successful API response is lost or storage fails before progress is committed. Expand actual IndexedDB interruption tests to failed writes, tab closure during requests, and account switches.
- [ ] Replace automatic `db:deploy || db:push` startup fallback after rehearsing migration history on a restored production backup. Failed migrations must not silently switch to schema synchronization; retain disposable-development recovery as an explicit opt-in command only.
- [x] Migrate Docker/CI/local tooling and package engines from end-of-life Node 20 to Node 24 LTS; add `.nvmrc`. Node 24.20.0 builds, Prisma generation/migrations, Alpine Chromium PDF rendering, and full integration suite passed (`logs/test-20260906-164021.txt`). Production-build browser validation remains a release gate. See [Node.js release support](https://nodejs.org/en/about/previous-releases) and [EOL policy](https://nodejs.org/en/about/eol).
- [ ] Audit refrigerant calculations and every report against its advertised scope: usage, both recovery categories, transfers, archived tanks, filters, and missing measurements. Compare outputs with known fixtures, not just successful downloads.
- [ ] Audit availability/directory reconciliation across properties, NTV occupancy, stale reports, missing units, and occupied-board cleanup. Preview must describe exactly what will change.
- [x] Correct Mark Ready's substring classification: NOT LEASED no longer becomes LEASED. Only known vacant statuses gain readiness; NTV, occupied, down, custom, and missing statuses are preserved rather than inventing leasing/occupancy facts. Pure fixtures pass, and the integration workflow verifies persisted leased/unleased and both NTV records. Local only; not deployed.
- [ ] Unify availability status normalization across imports, section placement, conflict previews, and UI occupancy checks. Audit underscore/hyphen variants and ambiguous legacy VACANT_LEASED semantics; substring READY checks must not misread NOT_READY as ready.
- [ ] Trace each save through response, refresh, activity history, and export. Distinguish loading, empty, failed, stale, and confirmed states.
- [x] Correct dashboard vendor overdue to compare with today, align planned/move-in windows with UTC calendar-date storage, and stop labeling NOT READY availability as marked ready. Known-date fixtures cover current-week overdue work, today/inclusive-start and exclusive-end bounds, and UTC/Chicago/Auckland environments. Local only; not deployed.
- [ ] Finish dashboard occupancy/date auditing: preserve physical occupancy counting of both NTV variants, add unknown-occupancy fixtures, and decide whether property-local timezone configuration is required rather than the current shared UTC calendar convention.
- [ ] Correct dashboard recent-status event sources: scheduled move-out/vacated dates are not evidence that a notice was logged or edited within the last 24 hours. Use actual event/audit timestamps, exclude future timestamps, and distinguish archived records from confirmed move-ins.
- [x] Separate overview totals from recent-row samples. Project counts/cost totals use all scoped lightweight records; priority/upcoming/photo queries are filtered before their ten-card limits. PM totals use all lightweight task summaries and load details for at most thirty visible cards. Isolated 301-project and 201-task fixtures verify totals and late priority/completion records; full integration validation remains pending for this change.
- [x] Disclose unit-history scope: timeline responses expose truncation metadata using one-extra-row checks; both screens identify the recent preview and its 250-event/200-audit/20-automation-per-turn limits. JSON exports include scope metadata and the event CSV button says Recent Events. Scoped route fixtures verify each truncation source and unauthorized property access. Local only; not deployed.
- [ ] Add a paginated or asynchronous complete unit-history export. Current previews deliberately remain bounded and are not full audits; visual previews show fewer rows than their downloadable loaded-event snapshot.
- [x] Replace the destructive global runtime-error screen with a dismissible notice outside the React root. Render errors use React's uncaught-error callback; details are text, not HTML. The isolated browser test covers preserved drafts, repeated errors, markup escaping, mobile width, dismissal, and cancelled reload.
- [x] Scope refrigerant controlled-form resets to the submitted form using its reset event. The local browser fixture verifies an unsubmitted charge draft survives a separate successful type creation.
- [x] Complete the full local `test.sh` suite with the dependency repairs below. Root/API/web audits, builds, and authenticated API workflows passed on 2026-09-06; the latest repeat includes unit-scope and lease-default initialization fixes (`logs/test-20260906-161042.txt`). Pushed GitHub CI is not yet verified.

## P1: Predictable Field Work

- [x] Count Lease dashboard totals across all scoped issues, independently of recent activity. Separate bounded queries keep older notice/violation work visible; resolved totals exclude future resolution dates and overdue totals exclude resolved/archived records. A deterministic fixture reproduced zero open issues when 40 recent resolved records hid older work; it now passes alongside real-database totals, property/archive exclusion, preview queues and rendered KPI checks (`logs/e2e-20260909-041816.txt`, 3/3). Local, not deployed.
- [ ] Reconcile Lease aging across property-wide and all-property views. The selected-property overview uses its warning days; unspecified-property overview still uses the existing seven-day fallback. Review property-local civil-day aging, per-property settings in aggregate views, daysOpenOverride, and terminology separately. Summary counts and recent lists are separate live reads, not an atomic historical report snapshot.

- [x] Populate Pest and Lease assignee pickers from property-scoped overview staff, not `adminUsersQuery`. Server-side active/role/property filters match each module's assignment policy; responses contain only ID/name/role and no choices for read-only or unspecified-property requests. Existing overview query caching retains choices on failed refresh. Thirteen route checks pass; real browser saves pass for fresh TECH, LEASING and ADMIN sessions without an Admin users request, excluding other-property staff and verifying minimal response fields (`logs/e2e-20260909-035128.txt`). Local change, not yet deployed.

- [ ] Verify exact-unit matching and independent resolve/persist actions for every compliance issue in Quick Capture and Active Issues.
- [x] Give lease Quick Capture its own paginated exact-unit lookup, independent of the filtered 200-row Active Issues response. Mobile regression verifies multiple pages with an empty main list and resolving one issue without hiding the other. Resolved/archived issues are excluded from repeat cards; failed lookup shows retry feedback.
- [x] Extend independent paginated lookup to building/area-only grounds capture. API tests verify property scope and unit exclusion; mobile-sized browser checks find later-page cards with an empty Active Issues list. Pagination and cancellation across other capture modules remain queued.
- [x] Replace truthiness-based boolean query coercion across operational modules: URL values `false` and `0` no longer enable archive/inactive/recurring filters. Shared parser tests and custom-field archive lifecycle integration checks pass, including malformed-value rejection. Full integration pass: `logs/test-20260906-162321.txt`.
- [x] Correct planning's UTC date-only comparisons: same-day blocks disappeared when the API ran in a timezone behind UTC. UTC/Chicago/Auckland and DST date tests pass; the real-browser create-and-display check passes.
- [x] Enforce assignee property access in planning even for unrestricted admins, and revalidate the existing assignee when moving a block. Isolated route tests verify rejection before writes and correct 403/404 distinctions. Non-manager patchable fields and canceled-block visibility remain queued.
- [x] Bring direct board assignment validation up to planning's property-scope standard. Creation, single edits and bulk assignment now require one active eligible staff record covering all target properties; see the earlier verified scope checkpoint above. Remaining work is property-filtered pickers and stable user-ID migration for ambiguous names, not the already-fixed direct property check.
- [x] Keep expected login failures local to the sign-in form and block duplicate pending submissions. Browser regression verifies preserved inputs, inline error without global notice, and successful retry.
- [ ] Audit bulk archive/delete selection scope, confirmation, double-submit prevention, and restore access on mobile.
- [x] Clear hidden selections when visible rows change, snapshot archive and section-move targets with property/unit labels, prevent repeated confirmation, and preserve failed confirmations for retry. Browser regressions change the selection behind open confirmations and verify only the original target is submitted. Snapshot enhancements are local only; direct bulk assignment, status changes, and restore still need pending/error-state auditing.
- [x] Guard direct board bulk actions against concurrent submissions and catch failures inline on desktop/mobile. Preserve failed selections and retain newly selected, unsubmitted rows after success. Mobile assignment/status failure and retry regressions pass in the full 82-check browser suite. A dedicated restore failure browser fixture remains separate. Local only; not deployed.
- [x] Prevalidate every archive/restore destination before updates and submit all record changes in one Prisma transaction. Two-property route fixtures verify missing destinations cause no earlier write and failed transactions do not produce success audits. Full API/PDF integration passed; a fault on the second archive record in disposable PostgreSQL verifies rollback, followed by successful archive/restore retries. Local only; not deployed.
- [ ] Separate notification/risk/audit failures from confirmed bulk mutations so retry messages do not imply rollback. Archive real-database rollback is verified; add restore-specific fault injection and post-commit notification-failure tests. Review concurrent lifecycle-section changes as well as record-write failures.
- [ ] Standardize form grids, checkbox rows, dropdown portals, dialogs, and action rows. Add viewport and keyboard regression coverage before replacing module-specific CSS.
- [ ] Test durable local saves and attachment queues under disconnection, reload, retries, authentication expiry, storage failure, and duplicate submission. Do not clear capture until persistence succeeds.
- [x] Bind offline jobs to their initiating user and verified session, including automatic sync, manual retry, queue visibility, account switches and server-side expected-account checks. Legacy ownerless jobs are held without reassignment or deletion. Full 151-browser gate passed in `logs/e2e-20260909-003944.txt`; API/build/lint/audit gate passed in `logs/test-20260909-003658.txt`. Legacy recovery UI and server idempotency remain separate work. Local, not deployed.
- [x] Share a same-tab per-job lock between manual retries and automatic sync. Three failing concurrency fixtures now pass, along with retry-after-rejection and earlier offline/storage lock-release checks. Queue deletion/failure updates are inside the shared lock.
- [ ] Extend offline delivery coordination across browser tabs and require server idempotency for retries after ambiguous responses. Same-tab locking does not prevent duplicate records after a lost response, partial upload, reload, or concurrent tab.
- [x] Clear all React Query entries on successful sign-out/sign-in, including session-expiry reauthentication. Same-tab admin-to-tech browser regression verifies prior cached lease cards do not appear while the new account's lookup is pending. Offline job ownership is now separately verified above; ambiguous-delivery idempotency remains open.
- [x] Release the offline sync lock after offline exits and initial storage failures. Isolated tests verify a subsequent online or storage-recovered attempt can run; this does not yet cover durable attachment delivery.
- [ ] Remove redundant entry where prior values are reliable, and label gross/net/initial/remaining weights explicitly. Unknown must not silently become zero.

## P2: Simplification

- [ ] Review role-specific landing pages and actions with techs, leasing, and managers; hide irrelevant controls without weakening API authorization.
- [ ] Collapse optional context and empty related-record sections; keep primary capture and work actions visible.
- [ ] Validate report terminology and disclose scope/limits where completeness is not guaranteed.

## First Local Pass

- [x] Move refrigerant type/tank/charge/recovery/final-recovery form resets after confirmed saves. A local 412px browser fixture verified the type form retains values during pending/rejected writes and clears after success. Remaining form-specific field validation stays in the broader audit.
- [x] Add history loading/error/retry feedback. The local browser fixture verified failed history does not appear empty and retry recovers. Cached rows remain visible with the error message when available.
- Targeted authenticated browser tests for refrigerant failed saves/history, toolbar account/dialog behavior, and runtime-error preservation all passed against the isolated local test database. A broader browser run exposed cross-origin edit failures and stale test selectors; reruns are in progress. Web production build and `git diff --check` passed.
- [x] Fix full-audit property-label initialization, remove the 1,000-transaction export ceiling, and include the union of all CSV section columns. Preserve property labels in the full-audit unit-history section. Use escaped/quoted spreadsheet output for formula prefixes, tabs, and line breaks.
- [x] Fix virgin-tank remaining-weight math and low alerts: remaining means net contents for virgin tanks, free capacity for recovery tanks. API tests cover full/low virgin tanks with tare and unchanged recovery headroom.
- [x] Capture Pest Quick Add/vendor form elements before awaiting saves, and reset lease issue-type setup only in its success callback. Lease issue-type failures now stay beside the form, and pending saves disable the submit button. Authenticated mobile Pest and Lease setup failure/success browser checks passed.
- [x] Allow PUT/PATCH/DELETE CORS preflights for trusted origins. Browser column-label edits, photo markup, and operating-calendar saves now pass across separate web/API addresses. Untrusted origins remain denied; explicit preflight checks are included in `test.sh`.
- [x] Fill six missing English/Spanish translation labels and add a literal translation-key regression check.
- [x] Preserve typed search text when clearing a selected option or refreshing option objects. Stop map placement from immediately auto-selecting another unit during a search; retain automatic advancement after saved placements. Map upload/placement/reposition/drawer and unit-creation browser workflows passed.
- [x] Capture the map file input before awaiting upload, reset only on success, and show upload errors inline rather than creating an unhandled event error.
- [x] Block map upload during pending creation and unit search during pending placement, preventing delayed success callbacks from selecting an old map or overwriting the next search. Updated map browser tests pass with existing maps present.
- [x] Guard confirmation dialogs against repeated submission and show rejected actions inline. Keep failed board archive confirmations open; prune selections when rows leave the filtered board. Desktop failure/duplicate-confirm/filter tests pass; broader cross-property/mobile target-list review remains queued.
- [x] Move analytics comparison hooks before loading/empty returns. A delayed-response dashboard test reproduces and now prevents the hook-order crash. Full display-mode workspace checks and the mobile pool/pest/lease/project/PM/wiki/map workflow pass locally.
- [x] Ignore the API-local upload directory so runtime photos/documents are not accidentally committed.
- The isolated API report test uses 1,005 transactions across two properties and archived dirty inventory. It verifies CSV, spreadsheet, and HTML contents, late-record compliance errors, and property filtering. PDF rendering and real-database query behavior remain separate validation items.
- [x] Include legacy `RefrigerantLog` records separately, dismissed leak flags with resolution notes, and archived/disposition information in full audit. Disclose shared cylinder inventory versus property-filtered history. Isolated export tests cover these sections without double-counting legacy logs as modern transactions.
- [x] Correct stale refrigerant documentation (`TW` terminology, charge-source scope, calculation descriptions) and explicitly document the current recovery-ceiling estimate's limitations.
- [ ] Validate and replace the recovery-ceiling estimate using cylinder/refrigerant manufacturer requirements. Current WC/nominal-size math does not account for refrigerant density, temperature, or cylinder compatibility; do not treat it as a universal verified safe fill limit.
- [ ] Audit Pest capture interruption/partial-upload failure and repeated-submit behavior. Preserve the confirmed issue ID so recovery never requires recreating an already saved issue.
- [x] Apply compatible Fastify/fast-uri patches and web nanoid update. Replace the isolated Puppeteer PDF adapter with Playwright Core (existing Chromium executable, same paper/margin/header/footer options). Remove the vulnerable extract-zip chain. Scope a pinned deepmerge-ts 8.0.2 override to Prisma config; schema generation, all 45 migrations on a fresh disposable PostgreSQL database, and password lifecycle tests passed.
- [x] Verify multipage PDF text, headers, and page-number footers locally; verify PDF creation using Alpine's system Chromium in the API image. Root/API/web npm audits are now clean. Full CI and release verification remain separate gates.
- [x] Isolate `test.sh` under a unique Compose project before its volume cleanup and validate Compose configuration without writing secrets to logs.
- [x] Isolate both test scripts from deployment `.env`, inherited database/upload paths, and Compose project/file settings. Browser script now cleans up only its unique disposable project and runs shared-fixture tests serially; environment-isolation shell regression passes.
- [x] Complete the full desktop/mobile browser run against a fresh demo database: 63 tests passed on 2026-09-06. API integration suite also passed with planning, boolean filters, and unit lookup fixes (`logs/test-20260906-163104.txt`). Node 24 release validation remains in progress.
- [x] Reject blank/null/boolean/nonfinite required refrigerant weights rather than silently coercing them to zero. Numeric zero and optional null tare values remain supported.
- [x] Reject virgin/archived recovery targets, clean/dirty category mismatches, clean-refrigerant type mismatches, unrelated extra tank IDs, same-tank transfers, and repeated final recovery. Isolated API validation tests pass.
- [ ] Move refrigerant tank validation and inventory writes into a locked transaction; test concurrent charges/recoveries and stale scale baselines. Also verify unit/property consistency and permissions for administrative tank corrections.
- [ ] Surface gross readings below stored tare as discrepancies instead of silently clamping negative contents to zero. Distinguish measured from inferred tare and provide an audited correction path before deciding which legacy readings should block new entries.
- [ ] Model clean-recovery provenance and ownership eligibility explicitly. Current charge validation checks category, active status, and refrigerant type, but shared cylinder records do not establish originating equipment/ownership or validate reuse eligibility. Review the applicable requirements before claiming the workflow enforces them.
- [x] Validate refrigerant unit IDs against their directory property and canonical number. Isolated API tests cover missing records, mismatched properties, denied unit scope without a supplied property, and canonical values on successful recovery.
- [x] Make default lease issue-type creation duplicate-safe under simultaneous initial requests. Ten concurrent requests against PostgreSQL all returned the same unique default types.
- [ ] Add immutable tank-edit history with before/after weight, tare, capacity, category, and refrigerant values plus correction reasons. Include lifecycle events in audit exports; existing generic update messages cannot reconstruct historical tank configuration and must not be presented as doing so.
- [x] Add React hook-order linting to the CI test path and verify the rule rejects an early-return fixture. It found and fixed a second conditional-hook bug in the dashboard throughput section. Broader dependency/React Compiler lint adoption remains a separate cleanup.
- [x] Clear cached API responses on logout/account changes/authorization failures, exclude admin/export responses, ignore late prior-session responses, and preserve fresh responses on quota failures. Isolated worker tests and a real service-worker-enabled mobile sign-out/offline check pass.
- [x] Complete the Node 24 production-image browser run: 66 desktop, mobile, visual-capture, and PWA checks passed (`logs/e2e-20260906-165313.txt`). Mock-driven tests block service workers; the dedicated PWA test explicitly enables them.
- [x] Repeat the production-image browser run with common-area pagination, same-tab account switching, sync-lock repair, report cutoff fixes, and health-gated startup: 67 checks passed (`logs/e2e-20260906-171735.txt`). Planning/map privacy fixes were made after that image snapshot and have separate regression coverage.
- [x] Repeat production-image desktop/mobile/PWA validation with planning/map privacy, pool chemical validation, error/retry states, and spreadsheet fixes: all 69 checks passed (`logs/e2e-20260906-174428.txt`). Project/PM complete-total queries were added after that image snapshot and have focused coverage pending broader validation.
- [x] Keep building/area-only repeat matching separate from unit-specific issues. Pure matcher tests cover wrong units in the same building, cleared selection, common areas, and resolved/archived exclusion.
- [x] Reject delayed identity responses after sign-out and stop offline cache reads when cache deletion fails. Six isolated service-worker regressions pass.
- [x] Render and inspect the complete refrigerant audit PDF with 1,005 transactions, archived dirty recovery, legacy logs, and dismissed flags. Landscape page dimensions and extracted contents pass; raw text extraction preserves wrapped cell identifiers.
- [x] Remove silent issue/log cutoffs from lease, pest, and pool HTML/PDF reports. A 251-record fixture reproduces the prior failure and now verifies the final matching record in all nine CSV/HTML/PDF outputs while preserving property filters. Reports remain operational summaries, not complete attachment or lifecycle histories.
- [x] Validate pool chemical references against the active property catalog, reject incompatible measurement units, and store canonical chemical names. Pure and route tests cover cross-property/unknown IDs, invalid liquid/solid units, gallons/quarts, and equivalent pounds/ounces saves.
- [x] Repeat the full API integration suite with report completeness, planning privacy, map privacy, and custom-port configuration checks: `logs/test-20260906-172307.txt`. Pool chemical validation was added afterward and has separate focused coverage pending the next full run.
- [x] Wait for API HTTP health before starting the web proxy and keep container port 4000 independent of the published host port. Fresh default-port integration (`logs/test-20260906-171145.txt`) and an isolated 14999/18080 startup/proxy check pass. Host boot validation remains separate.
- [x] Remove the daily manager report's silent 500-event cutoff. A 501-event regression verifies JSON/CSV completeness, date boundaries, and property authorization.
- [x] Escape spreadsheet formula prefixes in daily, lease, pest, pool, project, PM, board, map, and charge CSV exports. Project/PM/map tab-delimited spreadsheet exports now quote embedded tabs, newlines, and quotes; route fixtures verify hostile and multiline text, including grouped charge reports.
- [x] Prevent custom-field labels from overwriting built-in board CSV keys or same-label custom fields. Preserve unique existing headers; collisions receive a custom-field ID suffix. Pure helper and actual CSV route tests verify duplicate labels, built-in preservation, deterministic ordering, generated-name collisions, and formula escaping. Local only; not deployed.
- [x] Full local API integration passed again (`logs/test-20260906-174112.txt`), covering pool validation, daily completeness, and project/PM export updates. The subsequent board/map/charge escaping changes have focused route coverage and await the next full integration pass.
- [x] Full API integration passed with board/map/charge spreadsheet escaping and complete project/PM overview totals (`logs/test-20260906-175014.txt`). Same-tab retry locking was added afterward and has six focused orchestration regressions.
- [x] Distinguish failed pool-history and project-list requests from empty results, preserving cached rows and offering retry. Both 412px browser failure/retry regressions pass.
- [x] Disclose bounded risk evaluation coverage in responses and audit metadata: deterministic first-500 selection, one-extra-row truncation detection, selection scope, and skipped counts. Route fixtures cover 501/500/zero candidates and property rejection. Local only; not deployed.
- [ ] Add complete batched/background risk evaluation. Repeating the bounded default request does not advance to another page; scope metadata is disclosure, not complete processing. Add notification/load tests before removing the synchronous cap.
- [x] Reject risk-evaluation item IDs that do not belong to an explicitly selected property, even when the caller can access both properties. Manager/admin mismatch tests reject before evaluation; a matching request still reaches the evaluator.
- [x] Make audit IP attribution honor `TRUST_PROXY`, using the same helper as authentication. Untrusted forwarding headers no longer override the request IP in audit records; tests cover trusted/untrusted modes and background events.
- [ ] Restrict proxy trust to the deployed proxy topology rather than trusting arbitrary forwarding headers whenever a boolean is enabled. Review exposed API ports and Cloudflare-to-origin forwarding before rollout; generic proxy-enabled behavior remains unchanged by the audit fix.
- [ ] Add report-size/load testing and an asynchronous export path for exceptionally large histories; never silently truncate matching records as a memory workaround.

Dependency patch references: [Fastify advisory](https://github.com/advisories/GHSA-w2qp-rph6-63g4), [fast-uri advisory](https://github.com/advisories/GHSA-5jgf-p345-68v8), [nanoid advisory](https://github.com/advisories/GHSA-2v37-7h3g-55p8).

Compatibility references: [deepmerge-ts 8 changes](https://github.com/RebeccaStevens/deepmerge-ts/releases/tag/v8.0.0), [Playwright PDF API](https://playwright.dev/docs/api/class-page#page-pdf). The Prisma override is deliberately limited to its config loader; reassess it when upgrading Prisma.

## Release Gate

- Deployed UI audit code `3782b96` to mr-os.com on 2026-09-07, web-only update. Full browser suite: 94/94 passed; lint passed. Live login, dialog focus, final-walk settings and Pool Log/mobile smoke checks passed without work-record mutations. API/database remained healthy and running; no schema changes. Verified server backups: `backups/makereadyos-db-20260907-065747.dump`, `backups/makereadyos-uploads-20260907-065748.tgz`; rollback web image `rollback-405ca50`. Docker/app/Cloudflare boot services still enabled. Larger audit findings remain in `END_USER_UX_AUDIT.md`.

- 2026-09-07 UI audit release candidate: 94/94 production-image browser tests passed (`logs/e2e-20260907-005907.txt`); lint and diff checks passed. Includes reference-panel error recovery/empty-state cleanup, focused final-walk actions and unsaved inspector-order protection, local pool form errors, and shared-dialog keyboard focus handling. No API or schema changes. User authorized push/deployment; see `END_USER_UX_AUDIT.md` for remaining larger findings.

- Deployed application code `63566e8` on 2026-09-07 to mr-os.com: calendar start/finish separation, playful pond, final-walk inspector hierarchy, and compact library preview cards. Code CI `34086979992` passed. Full browser run `logs/e2e-20260907-002759.txt` passed 88/89; the remaining lease assertion matched an ancestor section containing both a heading and recent summary. Test-only fixes culminated in `56b3ee6`, with all five mobile workflows passing (`logs/e2e-20260907-003651.txt`). Full API/PDF/build/lint gate remains `logs/test-20260907-001746.txt`; subsequent application changes were CSS only. Applied `20260907060000_final_walk_assignment` explicitly with `db:deploy`, without schema-push fallback. Fresh database backup `backups/makereadyos-db-20260907-003537.dump`, earlier verified dump `backups/makereadyos-db-20260907-002832.dump`, upload archive `backups/makereadyos-uploads-20260907-002900.tgz`, and rollback images `rollback-8f086b5` retained on the server. Live public login, pond/unit navigation, final-walk settings reads, and compact preview passed with no uncaught browser errors; unauthenticated auth/me correctly returned 401. No property inspector/scheduling settings were enabled by verification. Docker, makereadyos@mros, and cloudflared remain boot-enabled. Temporary verification credentials were removed locally after the smoke test. Existing follow-up limitations below remain unchanged.

- Local library preview layout fix, 2026-09-07: mapping/preview cards and nested lists align content at the top instead of stretching short entries across the tallest column. Responsive columns can shrink to container width and long keys wrap. Production build and preview/install browser regression passed at 1600px and 412px (`logs/e2e-20260907-002624.txt`); checks enforce compact heading/description/list gaps, content-sized first cards, and no mapping-grid overflow. Screenshots inspected. CSS-only application change; not pushed or deployed.

- Local final-walk assignment checkpoint, 2026-09-07: property inspector order, independent ready-only My Work assignments, preference-aware in-app notifications, transactional audited handoffs, and assigned-inspector signoff (including leasing). Future dates alone do not create assignments. Stale handoffs, out-of-scope access, former-inspector signoff, and exhausted chains are rejected. Existing chains are preserved on policy edits. Full API/PDF/build/lint gate passed (`logs/test-20260907-001746.txt`); desktop flow passed (`logs/e2e-20260907-001555.txt`); final 412px setup/handoff/signoff passed (`logs/e2e-20260907-002224.txt`) and screenshots were inspected. The intermediate mobile test used a desktop-only heading assertion; corrected to the actual mobile navigation. Calendar and pond regressions passed in `logs/e2e-20260907-002033.txt`. Migration `20260907060000_final_walk_assignment` is required before deployment. No push or deployment in this batch. Native JSON transfer of policies/inspection blocks and replacement of exhausted existing chains remain documented follow-ups in TURN_SCHEDULING.md; PostgreSQL backups preserve all state.

- Local pond refresh on 2026-09-07: bounded roaming, swimming tadpoles, feeding reactions, greetings separate from opening work records, and a four-outfit collection. Ready-unit thresholds exclude not-ready units; unlocks remember the peak visible ready count, not lifetime completed turns. Collection data is account-scoped in this browser only. Motion pauses for hover/focus, arranging, hidden tabs, and reduced-motion preferences. No server data or work records are changed by pond play. Production web build, lint, and all four focused Chromium checks passed (`logs/e2e-20260907-000357.txt`), including actual feeding animation/outfit sprites, saved discoveries, mobile width, reduced motion, and no work-record mutations. Desktop/mobile screenshots were inspected. The test scroll target was subsequently changed from the moving frog to the stationary scene to avoid unnecessary stability waits. No full-suite rerun in this UI batch; not pushed or deployed.
- [ ] Sync pond discoveries across devices through account storage, with server-verified milestones if rewards later become competitive. Current local cosmetics are deliberately noncompetitive and not an authoritative completion history.

- Paused at user request on 2026-09-06: all current changes remain local and uncommitted; nothing pushed or deployed in this batch. Latest full API/PDF integration passed (`logs/test-20260906-200122.txt`), including Mark Ready leasing/occupancy preservation and real-database archive rollback. No test processes remain running.
- Local browser checkpoint: all 82 production-image checks passed (`logs/e2e-20260906-194703.txt`), including direct bulk-action retry guards, confirmation target snapshots, mobile/PWA paths, and IndexedDB reload. The later Mark Ready and transactional archive API changes are covered by the latest API/PDF integration, not that earlier browser image. No push or deployment.
- Full local API/PDF integration passed (`logs/test-20260906-193421.txt`) with risk coverage, invite authorization/delivery failures, pool validation, and confirmed offline checkpoints. The subsequent section-move browser fixture passes against the local app; broader browser coverage for this final change remains pending. No push or deployment in this batch.
- Local checkpoint on 2026-09-06: all 79 production-image browser checks passed (`logs/e2e-20260906-192738.txt`), including invite controls, mobile header/PWA paths, incomplete pool entries, and actual IndexedDB reload/retry. The later risk-evaluation coverage metadata has focused route coverage and is entering full integration validation. No changes in this local batch have been pushed or deployed. Dedicated Chromium PDF fixtures for multipage rendering, operational reports, and refrigerant exports also passed.
- Deployed code release `f2d7d6132deefcb440f29e126860954b4c94dff1` on 2026-09-06. Final full API/PDF suite passed (`logs/test-20260906-180406.txt`); all 73 production-image browser checks passed (`logs/e2e-20260906-180642.txt`); GitHub CI run `34065978161` passed. Server smoke checks passed for login, meta, project/PM/pool/refrigerant overviews, planning capacity, map pins, and full refrigerant CSV/PDF. Public HTML/assets returned 200 and unauthenticated auth/me returned 401. Runtime is Node 24.20.0; boot services remain enabled. Database/upload backups and prior images were retained. No pending migrations were applied and no schema synchronization ran. Existing schema differences and remaining unchecked TODOs are not resolved by this release.
- Release preparation on 2026-09-06: final API integration, builds, audits, and actual Chromium PDF regressions passed (`logs/test-20260906-180406.txt`). A test-only readonly-config assignment was corrected before this pass. Production database/uploads backups were verified before deployment; all 45 migration checksums match, with pre-existing live-schema differences queued for separate reconciliation. This release changes no Prisma schema or migration files. Final 73-test production-image browser verification is running before server cutover.
- Paused at user request on 2026-09-06. All changes remain local and uncommitted; nothing pushed or deployed during this audit. Latest full API integration: `logs/test-20260906-175014.txt`. Latest full production browser run: 69 passed (`logs/e2e-20260906-175233.txt`), including complete project/PM totals and same-tab retry locking. Subsequent risk-scope and audit-IP fixes pass focused tests. PM calendar/tasks/templates/history now distinguish loading/failure from empty results, preserve cached rows, and offer retry; all four 412px browser regressions passed. Lint and diff checks pass. Run the full integration/build/browser suite again for these final changes before release.
- [ ] Build and run targeted regression tests for each change; record untested scenarios explicitly.
- [ ] After server hardware work is complete, verify boot, installed RAM, app health, and field workflows before deployment.
- [ ] Preserve database/uploads backups before any data or schema changes. No production mutations during local auditing.
- [ ] Audit host boot management against Compose restart policies before changing restart behavior; avoid competing host-level supervisors. API health checks verify HTTP startup, not continuous database connectivity. See [Docker restart policy guidance](https://docs.docker.com/engine/containers/start-containers-automatically/).
# Pond Ecosystem And Theme Secrets (2026-09-07)

Local expansion adds seven pixel wildlife visitors, a 27-entry field guide and dated discovery journal, one interactive secret for each of the 15 backgrounds, a three-distinct-day sunflower garden, barista apron and optional rare palettes, selectable seasonal decoration, adult call-and-response, synchronized hops, nap-buddy observations, occasional fly misses and consolation snacks. Existing fly sprites, species-specific food, natural outfits, motion preferences and opt-in audio remain. No work-record mutation or server dependency was introduced.

Production-image browser checks passed: `logs/e2e-20260907-175135.txt` (5/5, unlocks/persistence/mobile/garden/audio/dropped flies) and `logs/e2e-20260907-175418.txt` (6/6, final build including wildlife active-time cycle, adult chorus, pause, food alternation and bounded journeys/tadpole cues). Build, lint and diff checks passed. Desktop/mobile screenshots reviewed; apron placement corrected after the first screenshot. Miss/consolation choreography and perceived audio quality still benefit from manual play on the target device; the automated suite does not force every ambient encounter. Not committed, pushed or deployed.

# Pond Wardrobe And Stationary Routes (2026-09-07)

Added fixed three-pad adult routes, species-specific food and selected-creature feeding, deterministic personalities with distinct travel/rest cadence, persistent per-creature outfits, discovery timestamps with honest legacy-date fallback, outfit previews, and a pond-only view with Escape/focus restoration. Natural pond clears personal outfit overrides. Browser coverage passed for the new wardrobe/species/scrapbook flow, natural/Rodeo behavior, route invariants and the broader living pond (`logs/e2e-20260907-160217.txt`, four passing tests); the catch timing assertion was made resilient by removing a screenshot during its short animation and passed on rerun (`logs/e2e-20260907-160436.txt`). Build, lint and diff checks passed; stationary pixel-pad screenshot reviewed. All changes remain local and uncommitted, including the earlier pending pixel-pad/catch/natural fixes. No work records or server data were changed.

# Natural Pond And Rodeo Frogs (2026-09-07)

Natural pond now uses only plain green/brown sheets, independent of work status. Cowboy hats are an optional Rodeo frogs collection reward unlocked at three simultaneously ready accessible units; unlocking does not equip it. The focused production-image test verifies two-unit lock, three-unit unlock, hat-free natural mode under assigned/major/critical statuses, explicit equip, reload persistence and returning to natural (`logs/e2e-20260907-155408.txt`, 1/1 passed). Build, lint and diff checks passed. Local only, alongside the pending pixel-pad and fly-catch changes.

# Pixel Pads And Fly Catches (2026-09-07)

Local pixel-art lily/ripple assets replace rounded gradients, with integer sizing and stepped bobbing. Fly catches now show a tongue, fly retraction, and nom/chewing feedback before removal; only awake adult frogs within a 36-pixel radius hunt, one catch per frog. Motion pause clears transient effects. Production-image tests passed 3/3 (`logs/e2e-20260907-155058.txt`), covering eligibility, visible catch stages, reduced-motion cleanup and the broader living-pond workflow. Build, lint and diff checks passed; pixel pads and catch screenshots visually reviewed. Not pushed or deployed.

# Living Pond Expansion (2026-09-07)

Added bounded journeys with rests, targeted three-neighbor feeding, species-specific tadpole bubbles/nibbles, naps and neighbor reactions, quiet labels with an always-visible option, local-time lighting, decorative rain, a discoverable dragonfly with a cosmetic reward/journal, observed ready-transition celebrations, and opt-in synthesized greeting sounds (off each visit). Playing does not edit work records. Fly collision filtering now returns existing state when no fly was caught, removing a render-loop risk. See FROG_POND_LIFE.md for behavior and limits. Production-image browser tests passed 6/6 (`logs/e2e-20260907-143948.txt`), including a minute of simulated pond time, no console/runtime errors after authentication, visitor persistence, mobile width, readiness transition, and reduced-motion override/restoration. Production builds, lint and diff checks passed; scene screenshot visually reviewed. Audio perceptual quality remains a manual device check. Local only, not pushed or deployed.

# Pond Motion Controls (2026-09-07)

The motion button previously displayed the saved animation preference even when the device's reduced-motion preference stopped both sprite ticks and CSS animations. It now reports effective motion and the pause reason. A deliberate, visit-only "Play animations anyway" action overrides reduced motion for this pond only; restoring the device preference stops it again. Device preference changes clear the override. Production-image browser regression verifies painted sprite variation, frame/action cycling, pause/resume, reduced-motion default, explicit override and restoration (`logs/e2e-20260907-141700.txt`); lint and builds passed. Local only. The user's device-specific static pond has not been conclusively reproduced; their report of "Pause" is consistent with the old misleading control but does not establish reduced motion as the cause.
